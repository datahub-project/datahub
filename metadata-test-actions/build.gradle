plugins {
    id 'java'
}

dependencies {
    compile project(":metadata-test-models")
    compile project(":metadata-io")

    compileOnly externalDependency.lombok
    annotationProcessor externalDependency.lombok
}

test {
    useJUnitPlatform()
}