source:
  type: hex-v2
  config:
    # Required — find your workspace name in your Hex URL: https://app.hex.tech/<workspace_name>
    workspace_name: your-workspace-name
    # Required — Workspace Token or PAT from your Hex workspace settings
    token: ${HEX_TOKEN}

    # Optional — path to the Hex CLI binary (default: "hex", must be on PATH)
    # hex_cli_path: hex

sink:
  # sink configs
