source:
  type: "flink"
  config:
    # JobManager REST API endpoint
    connection:
      rest_api_url: "http://localhost:8081"
      # Optional: SQL Gateway for catalog metadata extraction
      # sql_gateway_url: "http://localhost:8083"

    # Optional: filter jobs by name
    # job_name_pattern:
    #   allow:
    #     - "^prod_.*"

    # Optional: extract catalog metadata (requires sql_gateway_url)
    # include_catalog_metadata: true
    # catalog_pattern:
    #   allow:
    #     - "^my_catalog$"

    env: "PROD"

sink:
  # sink configs
