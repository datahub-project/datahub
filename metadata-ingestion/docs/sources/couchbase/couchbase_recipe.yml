source:
  type: couchbase
  config:
    connect_string: "couchbases://127.0.0.1"
    username: "Administrator"
    password: "password"
    cluster_name: "testdb"
    classification:
      enabled: true
      classifiers:
        - type: datahub
    profiling:
      enabled: true

sink:
  # sink configs
