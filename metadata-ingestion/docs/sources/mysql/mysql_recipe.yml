source:
  type: mysql
  config:
    # Coordinates
    host_port: localhost:3306
    database: dbname

    # Credentials
    username: root
    password: example

    # If you need to use SSL with MySQL:
    # options:
    #   connect_args:
    #     ssl_ca: "path_to/server-ca.pem"
    #     ssl_cert: "path_to/client-cert.pem"
    #     ssl_key: "path_to/client-key.pem"

    # AWS RDS IAM Authentication (alternative to password)
    # use_rds_iam: true
    # aws_region: us-west-2
    # # When use_rds_iam is true, the password field is ignored
    # # Ensure AWS credentials are configured via AWS CLI, environment variables, or IAM role

sink:
  # sink configs
