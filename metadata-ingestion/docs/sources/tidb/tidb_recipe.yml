source:
  type: tidb
  config:
    # Coordinates
    host_port: localhost:4000
    database: dbname

    # Credentials
    username: root
    password: example

    # If you need to use TLS with TiDB:
    # options:
    #   connect_args:
    #     ssl_ca: "path_to/server-ca.pem"
    #     ssl_cert: "path_to/client-cert.pem"
    #     ssl_key: "path_to/client-key.pem"


# sink configs
