source:
  type: "cassandra"
  config:
    # Coordinates
    contact_point: "localhost"
    port: 10350

    # Credentials
    username: "admin"
    password: "password"

    # Options
    excludeKeyspaces:
      - system
sink:
  type: "datahub-rest"
  config:
    server: "http://localhost:8080"