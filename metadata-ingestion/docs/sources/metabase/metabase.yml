source:
  type: metabase
  config:
    # Coordinates
    connect_uri: http://localhost:3000

    # Credentials
    username: user
    password: pass

    # Options
    default_schema: public

    # Optional mapping of platform types to instance ids
    platform_instance_map: # optional
      postgres: test_postgres

    # Optional mapping of database IDs to database names
    database_alias_map: # optional
        "2": "my_database"

sink:
  # sink configs
