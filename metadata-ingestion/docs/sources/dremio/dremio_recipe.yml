source:
  type: dremio
  config:
    # Coordinates
    hostname: localhost
    port: 26257

    # Credentials
    authentication_method: password
    username: user
    password: pass

sink:
  # sink configs