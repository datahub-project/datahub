source:
  type: "ldap"
  config:
    # Coordinates
    ldap_server: ldap://localhost

    # Credentials
    ldap_user: "cn=admin,dc=example,dc=org"
    ldap_password: "admin"

    # Options
    base_dn: "dc=example,dc=org"

    # TLS Security - Enable certificate verification (recommended for production)
    # Prevents Man-in-the-Middle attacks (CWE-295)
    # tls_verify: true  # Default: false (for backwards compatibility)

sink:
  # sink configs