source:
  type: preset
  config:
    # Coordinates
    connect_uri: https://abcdef01.us1a.app.preset.io
    manager_uri: https://api.app.preset.io

    # Credentials
    api_key: key
    api_secret: secret

sink:
  # sink configs
