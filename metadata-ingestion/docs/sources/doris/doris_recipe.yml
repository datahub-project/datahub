source:
  type: doris
  config:
    # Coordinates
    # Note: Doris uses port 9030, not MySQL's 3306
    host_port: localhost:9030
    database: dbname

    # Credentials
    username: root
    password: example

    # If you need to use SSL with Apache Doris:
    # options:
    #   connect_args:
    #     ssl_ca: "path_to/server-ca.pem"
    #     ssl_cert: "path_to/client-cert.pem"
    #     ssl_key: "path_to/client-key.pem"
# sink configs
