"""Embedding provider package.

Import concrete providers and helpers directly from their submodules — this
package intentionally does not re-export them (per the project convention to
avoid ``__init__`` re-exports).
"""
