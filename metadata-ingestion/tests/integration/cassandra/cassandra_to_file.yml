run_id: cassandra-test

source:
  type: cassandra
  config:
    ips:
      - localhost
    schema_pattern:
      deny:
        - "system"

sink:
  type: file
  config:
    filename: ./cassandra_mces.json
