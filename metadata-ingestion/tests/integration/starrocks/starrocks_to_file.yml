run_id: starrocks-test

source:
  type: starrocks
  config:
    host_port: "localhost:${STARROCKS_MYSQL_PORT}"
    username: root
    password: ""
    include_external_catalogs: false
    schema_pattern:
      allow:
        - "test_db"

sink:
  type: file
  config:
    filename: "./starrocks_mces.json"
