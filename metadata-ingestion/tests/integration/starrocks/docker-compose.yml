services:
  teststarrocks:
    image: starrocks/allin1-ubuntu:latest
    container_name: "teststarrocks"
    ports:
      - "9030"  # MySQL protocol exposed on a random host port
      - "8030"  # HTTP exposed on a random host port
