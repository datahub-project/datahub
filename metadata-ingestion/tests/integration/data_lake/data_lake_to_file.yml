run_id: data-lake-test

source:
  type: data-lake
  config:
    file: "./test.csv"
    file_type: "csv"
    # username: root
    # password: example
    # database: metagalaxy
    # host_port: localhost:53307
    # schema_pattern:
    #   allow:
    #     - "^metagalaxy"
    #     - "^northwind"
    #     - "^test_cases"
    # profile_pattern:
    #   allow:
    #     - "^northwind.*\$"
    #     - "^test_cases.*\$"
    # profiling:
    #   enabled: True

sink:
  type: file
  config:
    filename: "./data_lake_mces.json"
