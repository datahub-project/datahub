run_id: doris-test

source:
  type: doris
  config:
    username: root
    password: ""
    host_port: "localhost:${DORIS_MYSQL_PORT}"
    database: dorisdb

sink:
  type: file
  config:
    filename: "./doris_mces.json"

