run_id: doris-profile-test

source:
  type: doris
  config:
    username: root
    password: ""
    host_port: "localhost:${DORIS_MYSQL_PORT}"
    database: dorisdb
    profiling:
      enabled: true
      profile_table_level_only: false

sink:
  type: file
  config:
    filename: "./doris_mces.json"
