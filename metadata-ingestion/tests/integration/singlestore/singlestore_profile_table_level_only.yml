source:
  type: singlestore
  config:
    username: root
    password: example
    host_port: localhost:53308
    database: northwind
    profiling:
      enabled: True
      profile_table_level_only: true
sink:
  type: file
  config:
    filename: "./singlestore_mces.json"
