version: "3.3"
services:
  vertica:
    environment:
      APP_DB_USER: "dbadmin"
      APP_DB_PASSWORD: "vertica"
    container_name: vertica-ce
    image: vertica/vertica-ce
    ports:
      - "5433:5433"
      - "5444:5444"
    deploy:
      mode: global
    volumes:
      - type: volume
        source: vertica-data
        target: /data

volumes:
  vertica-data:


