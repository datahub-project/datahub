source:
  type: mysql
  config:
    username: root
    password: example
    host_port: "localhost:${MYSQL_HOST_PORT}"
    database: northwind
    profiling:
      enabled: True
      profile_table_level_only: true
sink:
  type: file
  config:
    filename: "./mysql_mces.json"
