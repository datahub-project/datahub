source:
  type: airbyte
  config:
    deployment_type: oss
    host_port: http://localhost:8000
    username: test@datahub.io
    password: "${AIRBYTE_PASSWORD}"
    verify_ssl: false
    extract_column_level_lineage: true
    platform_instance: custom-platform

sink:
  type: file
  config:
    filename: airbyte_platform_instance_mces.json