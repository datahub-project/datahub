source:
  type: mlflow
  config:
    tracking_uri: "http://127.0.0.1:5050"

sink:
  type: file
  config:
    filename: "mlflow_to_datahub.json"
  # type: datahub-rest
  # config:
  #   server: "http://localhost:8080"