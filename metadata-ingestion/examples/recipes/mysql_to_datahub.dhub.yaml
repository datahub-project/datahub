---
# see https://docs.datahub.com/docs/generated/ingestion/sources/mysql for complete documentation
source:
  type: "mysql"
  config:
    host_port: localhost:3306
    database: datahub
    username: datahub
    password: datahub

# see https://docs.datahub.com/docs/metadata-ingestion/sink_docs/datahub for complete documentation
sink:
  type: "datahub-rest"
  config:
    server: "http://localhost:8080"
