# see https://docs.datahub.com/docs/generated/ingestion/sources/ldap for complete documentation
source:
  type: "ldap"
  config:
    ldap_server: ldap://localhost
    ldap_user: "cn=admin,dc=example,dc=org"
    ldap_password: "admin"
    base_dn: "dc=example,dc=org"
    # Enable TLS certificate verification (recommended for production)
    # Prevents Man-in-the-Middle attacks (CWE-295)
    # tls_verify: true  # Default: false (for backwards compatibility)

# see https://docs.datahub.com/docs/metadata-ingestion/sink_docs/console for complete documentation
sink:
  type: "console"
