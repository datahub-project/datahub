name: Publish Datahub Client

on:
  workflow_run:
    workflows: ["build & test"]
    types:
      - completed

  release:
    types: [published, edited]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:

  check-secret:
    runs-on: ubuntu-latest
# Disable check for now while test fails
#    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    outputs:
      publish-enabled: ${{ steps.publish-enabled.outputs.defined }}
    steps:
      - id: publish-enabled
        if: "${{ env.SIGNING_KEY != '' }}"
        run: echo "::set-output name=defined::true"
        env:
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}

  publish:
    runs-on: ubuntu-latest
    needs: [check-secret]
    if: needs.check-secret.outputs.publish-enabled == 'true'
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - uses: actions/setup-python@v2
        with:
          python-version: "3.6"
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: us-west-2
      - name: publish datahub-client jar
        env:
          SIGNING_PASSWORD: ${{ secrets.SIGNING_PASSWORD }}
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
        run: |
          export CODEARTIFACT_AUTH_TOKEN=`aws codeartifact get-authorization-token --domain acryl --domain-owner 795586375822 --query authorizationToken --output text`
          echo signingKey=$SIGNING_KEY >> gradle.properties
          ./gradlew :metadata-integration:java:datahub-client:printVersion
          ./gradlew :metadata-integration:java:datahub-client:publish
