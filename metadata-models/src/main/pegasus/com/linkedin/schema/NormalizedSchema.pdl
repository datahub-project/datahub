namespace com.linkedin.schema

/**
 * The standard schema representation after normalization.
 */
record NormalizedSchema {

  /**
   * List of normalized schema fields.
   */
  normalizedFields: array[SchemaField]
}