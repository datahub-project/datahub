namespace com.linkedin.identity

import com.linkedin.common.Urn

/**
 * Settings related to the 'Views' feature.
 */
record CorpUserViewsSettings {
  /**
   * The default View which is selected for the user.
   */
  defaultView: Urn
}
