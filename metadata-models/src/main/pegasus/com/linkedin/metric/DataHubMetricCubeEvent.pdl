namespace com.linkedin.metric

import com.linkedin.timeseries.TimeseriesAspectBase

/**
 * A timeseries measure event, e.g. a single observation.
 */
@Aspect = {
    "name": "dataHubMetricCubeEvent",
    "type": "timeseries",
}
record DataHubMetricCubeEvent includes DataHubMetricCubeRow, TimeseriesAspectBase {
}