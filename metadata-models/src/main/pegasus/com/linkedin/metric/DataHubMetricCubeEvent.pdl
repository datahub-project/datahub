namespace com.linkedin.metric

/**
 * A timeseries measure event, e.g. a single observation.
 */
@Aspect = {
    "name": "dataHubMetricCubeEvent",
    "type": "timeseries",
}
record DataHubMetricCubeEvent includes DataHubMetricCubeRow {
}