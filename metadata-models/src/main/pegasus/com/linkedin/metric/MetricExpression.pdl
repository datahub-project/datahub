namespace com.linkedin.metric

/**
 * A metric's logical expression represented across one or more SQL dialects
 */
record MetricExpression {

  /**
   * The expression in one or more dialects.
   */
  dialects: array[DialectExpression]
}
