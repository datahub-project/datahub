namespace com.linkedin.common.fieldtransformer

/**
 * Field transformation expressed in UDF
 */
record UDFTransformer {
  /**
   * A UDF mentioning how the source fields got transformed to destination field.
   */
  udf: string
}