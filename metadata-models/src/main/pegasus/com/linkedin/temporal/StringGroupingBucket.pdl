namespace com.linkedin.temporal

/**
 * Defines a string grouping bucket.
 */
record StringGroupingBucket {
  /**
   * Key that specifies the column name to be used for bucketing.
   */
  key: string
}
