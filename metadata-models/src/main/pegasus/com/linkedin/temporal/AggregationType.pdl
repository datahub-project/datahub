namespace com.linkedin.temporal

/**
 * Defines if an aggregation should be on the latest value within a window or the sum.
 * TODO: Stop gap till we have the annotations.
 */
enum AggregationType {
  LATEST,
  SUM
}
