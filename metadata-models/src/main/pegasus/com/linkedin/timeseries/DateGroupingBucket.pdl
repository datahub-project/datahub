namespace com.linkedin.timeseries

/**
 * Defines a time based grouping bucket.
 */
record DateGroupingBucket {
  /**
   * Key that specifies the column name to be used as the timestamp field for bucketing.
   */
  key: string

  /**
   * Bucket size (like a day/hour etc).
   */
  granularity: CalendarInterval
}