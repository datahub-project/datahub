namespace com.linkedin.metadata.query.filter

/**
 * A set of Filters joined by Conjunction (AND).
 */
record ConjunctiveFilters {
  /**
   * A set of Filters to be AND'd together.
   */
  and: optional array[Filter]
}