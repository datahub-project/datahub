# Keep discovery enabled without waiting for peers in the local single-GMS stack.
# Existing JVM options follow the default so explicit values take precedence.
JDK_JAVA_OPTIONS=-Dhazelcast.wait.seconds.before.join=0 ${JDK_JAVA_OPTIONS:-}
