{
  "reviewer": "convention-reviewer",
  "connector_path": "metadata-ingestion/src/datahub/ingestion/source/snowflake/ (scoped to snowflake_openflow.py, snowflake_openflow_{config,models,query,report}.py, tests/{unit,integration}/snowflake_openflow/, docs/sources/openflow/)",
  "findings": [],
  "summary": {
    "critical": 0,
    "high": 0,
    "medium": 0,
    "low": 0,
    "notes": [
      "Mode: extend (per .connector-dev/mode). Rows 11/12/15 are 'new only' but _PLANNING.md explicitly overrides the extend-mode N/A for registration: this run adds a new extra (snowflake-openflow), a new entry point, a new openflow platform, and new docs -- all verified present, so these rows were reviewed as new rather than marked N/A, per the plan's own Preconditions #3.",
      "Row 1 (base class): StatefulIngestionSourceBase confirmed in source.py, matches _PLANNING.md architectural_decisions.base_class. Pass.",
      "Row 2 (file org + entry point): pyproject.toml has [project.entry-points...] entry 'snowflake-openflow = datahub.ingestion.source.snowflake.snowflake_openflow:SnowflakeOpenflowSource' (checked per precedence rule); setup.py also has it. File layout (source.py + _config.py + _models.py + _query.py + _report.py) matches patterns.md. Pass.",
      "Row 3 (config mixin coverage): only one ConfigModel/BaseModel subclass found in the four in-scope files (SnowflakeOpenflowSourceConfig), correctly inheriting StatefulIngestionConfigBase, DatasetSourceConfigMixin, LowerCaseDatasetUrnConfigMixin, using pydantic.Field throughout. No nested config models bypass the mixin. Pass.",
      "Row 4 (URN construction): zero manual f-string URNs (grep clean). Single primitive per entity family: dataset URNs (destination + upstream inlets) both go through make_dataset_urn_with_platform_instance (destination via SnowflakeIdentifierBuilder.gen_dataset_urn, upstream inline) -- no mixing with DatasetUrn(). DataFlow/DataJob built exclusively via SDK v2 DataFlow(...)/DataJob(...) constructors, no make_data_flow_urn or DataFlowUrn() mixed in. Pass.",
      "Row 5 (containers): gen_containers() used via OpenflowDeploymentKey/OpenflowRuntimeKey (ContainerKey subclasses), parent emitted before child in get_workunits_internal, matches containers.md exactly. Pass.",
      "Row 6 (SourceReport): SnowflakeOpenflowReport is a @dataclass inheriting StaleEntityRemovalSourceReport (itself a dataclass). Per-entity-type counters present for all 3+ declared entity types (num_deployments, num_runtimes, num_connectors, num_table_jobs). No TypedDict/Pydantic duplication found (grep clean). Pass.",
      "Row 7 (lint/mypy): independently re-ran ruff format --check, ruff check, and mypy --config-file setup.cfg against the four source files plus tests/unit/snowflake_openflow/ and tests/integration/snowflake_openflow/ -- all clean, 0 errors. Also re-ran the unit test suite: 226 passed. Confirms the stated 'mypy/ruff clean' and '232 tests pass' claims.",
      "Row 8 (test layout): golden file (snowflake_openflow_mces_golden.json, 20928 bytes, 34 events) lives in tests/integration/snowflake_openflow/, not under tests/unit/. No docker-compose.yml, matching the sibling snowflake/ integration tests' own pattern of mocking SnowflakeConnection.query() rather than running a live/dockerized Snowflake -- consistent with established family convention, not a deviation. Pass.",
      "Row 9 (banned constructs): grep for print()/time.sleep()/bare except/except Exception: pass -- zero hits in the four in-scope source files. Dead-method scan (9b) over every `def _foo` in source.py found zero methods with no `self._foo(` caller. Pass.",
      "Row 9b/retry-composition regression check (motivating this re-review): _query_rows is a bare seam; _query_rows_with_retry wraps it in exactly one _retrying() layer and is used by the inventory SHOW and by _paged_history; _read_connector_url and _download connector config path (_read_connector_config -> _retrying()(_download_connector_config, ...) -> _download_connector_config calls the bare self._query_rows once inside its own per-attempt TemporaryDirectory) each retry at exactly one layer. Verified by re-running test_stage_get_retries_a_transient_connection_error and the 'retried at exactly one layer, not nested' assertion in test_source.py -- both pass, confirming no double-retry composition remains.",
      "Row 10 (docstrings): SnowflakeOpenflowSource carries a full class docstring; decorators (@platform_name, @config_class, @support_status, @capability) all present with prose. Pass.",
      "Row 11 (docs): all four files present under docs/sources/openflow/ (README.md, snowflake-openflow_pre.md, snowflake-openflow_post.md, snowflake-openflow_recipe.yml) -- directory keyed on platform id 'openflow' per DataHub's actual docgen contract (docs/sources/<platform>/<plugin>_pre.md), confirmed against scripts/docgen.py's regex and against test_docs.py's 9 passing tests, including the bridging-registry enumeration test (test_supported_connector_types_table_matches_the_mapping) which pins CONNECTOR_DEFINITION_PLATFORM's 3 keys to the post.md table. New Limitations bullets ('Entity volume scales with replicated tables, not connectors' and the SHOW row-cap truncation caveat) are present in snowflake-openflow_post.md, matching the stated diff. Pass.",
      "Row 12 (installation plumbing): 'snowflake-openflow' extra present in both setup.py (_all_extras-reachable dict) and pyproject.toml [project.optional-dependencies]. Pass.",
      "Row 13 (foreign-platform URN joinability): upstream postgres/mysql/mssql identifiers are built by upstream_identifier(), which explicitly replicates each platform's own get_identifier() tier logic (DATABASE_SCHEMA_TABLE vs DATABASE_TABLE) with inline citations to postgres/source.py, mssql/source.py and kafka_connect's Debezium precedent, rather than importing a canonical helper (none exists for these DB platforms in the row-13 helper table, which only covers object stores). Destination Snowflake URNs go through SnowflakeIdentifierBuilder, the family's own canonical helper. Pass.",
      "Row 14 (source-type coverage, orchestration_tools.md): dataPlatformInstance/dataFlowInfo/dataJobInfo/dataJobInputOutput/ownership all emitted; globalTags correctly absent with an explicit @capability(TAGS, supported=False) matching the plan's stated reason (no tag column on any object view). Pass.",
      "Row 15 (platform registration): urn:li:dataPlatform:openflow entry present in data-platforms.yaml (line 752), and the data-platforms bootstrap step version was bumped to v18 in bootstrap_mcps.yaml in an earlier commit (a874e334cb9) in this same connector's history. No self-emission of dataPlatformInfo found in source.py (grep clean), matching the plan's 'bootstrap-mcp' decision. Pass.",
      "No findings against snowflake_v2.py, snowflake_queries.py, snowflake_summary.py or other pre-existing family modules, per scope."
    ]
  }
}
