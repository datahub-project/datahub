on:
  types:
    - dataset
  match:
    - query: editableDatasetProperties.description
      operation: equals
      params: "my params"
rules:
  - query: editableDatasetProperties.description
    operation: exists
    negate: true