# OpenSearch Configuration
OPENSEARCH_HOST=localhost
OPENSEARCH_PORT=9200
OPENSEARCH_USER=
OPENSEARCH_PASSWORD=

# AWS Configuration (for Bedrock)
AWS_PROFILE=acryl-read-write
AWS_REGION=us-west-2

# Application Settings
MAX_ENTITIES_TO_PROCESS=50
OUTPUT_FORMAT=console  # console, json, csv

# Embedding Configuration
# Provider for embeddings. Supported: bedrock | cohere
EMBED_PROVIDER=bedrock
# When provider=bedrock, choose the Bedrock model (defaults to Bedrock Cohere)
BEDROCK_MODEL=cohere.embed-english-v3
# BEDROCK_DIMENSIONS is only used for Titan v2 models (256, 512, 1024)
BEDROCK_DIMENSIONS=1024

# Cohere Configuration (used when provider=cohere)
COHERE_API_KEY=your_cohere_api_key_here
COHERE_MODEL=embed-english-v3.0  # or embed-english-light-v3.0 for faster/smaller

# Notes:
# - To use a specific AWS profile with Bedrock, set AWS_PROFILE before running
#   (e.g., export AWS_PROFILE=your-profile). Profile selection via kwargs is not
#   supported by the Bedrock API through LiteLLM calls; credentials and profile
#   are resolved via standard AWS environment/config files.
