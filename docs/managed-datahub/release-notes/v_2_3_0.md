---
description: "DataHub Cloud v2.3.0 release notes covering the Data Product Marketplace, the in-app billing dashboard, SPARQL in the Ontology Explorer, a much deeper Evals surface, metrics as first-class lineage citizens, and OpenSearch 3.x support."
---

# v2.3.0

## Release Changelog

### v2.3.0

#### Release Availability Date

TBD

#### Recommended Versions

- **CLI/SDK**: TBD
- **Remote Executor**: TBD
- **On-Prem Versions**:
  - **Helm**: TBD
  - **API Gateway**: TBD
  - **Actions**: TBD

#### New Feature Highlights

- **Data Product Marketplace** — a browsable marketplace for data products, with richer data product profiles and data products now filterable in global search.
- **Billing dashboard in the product** — the embedded Metronome iframe is replaced by a native usage dashboard, including spend forecasting to the end of the service period and provider-priced AI credit spend per run and per conversation.
- **SPARQL in the Ontology Explorer** — a SPARQL tab beside Explore that draws the entities a query binds; SELECT renders bound entities and their live relationships, CONSTRUCT renders its triples, ASK answers yes or no.
- **Evals, substantially expanded** — auto-generated evals, per-eval model selection, multiple reference outputs, automatic judging of simulated answers, per-run document-retrieval and query-validity checks, judge verdicts streamed through GMS, and Ask DataHub wired into the eval pages.
- **Metrics as first-class lineage citizens** — charts, dashboards and datasets can consume a Metric as an upstream; column-level lineage is modelled on metric upstreams; metrics and semantic models appear in global search.
- **Agent tasks can be triggered by metadata changes** — pick what to watch from a single "What changed" list, including new asset lifecycle events (created, deleted, restored).
- **Dark mode** — global styles and a dark palette across the app.
- **Two new ingestion sources** — IBM/HCL Informix and Monte Carlo.
- **OpenSearch 3.x support** — via a unified OpenSearch client shim.
- **MCP surface grows** — visualizations render as an MCP App, agents are served as call-and-poll tools behind a feature flag, and a new `evals` tool group ships.

- All changes in [https://github.com/datahub-project/datahub/releases/tag/TBD](https://github.com/datahub-project/datahub/releases/tag/TBD)
  - Note Breaking Changes: [https://datahubproject.io/docs/how/updating-datahub/](https://datahubproject.io/docs/how/updating-datahub/)

#### Product

- **Data Product Marketplace** — a dedicated marketplace UI for discovering data products, with profile enhancements on the data product page.
- **Data products in search** — data products are now filterable in global search.
- **Billing dashboard** — replaces the Metronome iframe with native dashboard components, adds usage forecasting to the end of the service period, and surfaces AI credit spend per run and per conversation. Includes usability follow-ups from the first internal demos.
- **Dark mode** — global styles plus an updated dark palette.
- **Branding** — logo drag-and-drop upload, and light brand colors updated for accessible contrast.
- **Favicon** — Appearance settings gains a dedicated **Favicon** field. The logo is reused as the browser-tab favicon only when it is a standalone symbol; if **"Logo includes organization name"** is enabled and no favicon has been set, the tab shows the default DataHub icon rather than squishing a wide lockup into a square. Upload a square favicon under **Settings → Appearance → Branding** to keep a custom tab icon.
- **Ontology Explorer** — gains the SPARQL tab described above, replaces search-match cycling with a results list that grows the picture as it is scrolled, docks the selected entity's sidebar beside the canvas instead of floating over it, and raises the canvas limit from 30 to 500 entities.
- **Code blocks** — an editable code block with SQL validation and formatting, plus a comparative view for side-by-side code blocks.
- **Compliance forms** — forms can be assigned to owners of a specific ownership type.
- **Documents** — proposal review is separated from direct document edits.
- **Deprecation dates** — the deprecation date input is now editable.
- **Evals in the UI** — eval proposals can be reviewed in the UI, evals can be auto-generated from the evals page, and context-generation jobs support schedules.

#### AI / Ask DataHub

- **Visualization widgets in Ask DataHub** — answers can render visualizations inline.
- **Ask DataHub sidebar across home pages** — the sidebar is shared across non-entity home pages, and a conversation URN can be copied from its Ask DataHub tab.
- **Ask DataHub sees AI-generated and propagated descriptions** — previously only human-authored descriptions were visible to it.
- **Bulk mutation caps** — Ask DataHub caps bulk metadata mutations and reports per-call coverage so a partial application is visible rather than silent.
- **Agent visibility** — agents marked "Show in Ask DataHub Chat" are now visible to all users, not only admins.
- **Evals** — backend support for auto-generating evals, multiple reference outputs in eval validation, per-eval model selection, automatic judging of simulated answers, per-run document-retrieval and query-validity checks, and judge verdicts streamed through GMS.
- **New `MANAGE_EVALS` platform privilege** — gates the Evals pages and eval actions.
- **MCP** — visualizations render as an MCP App; agents are served as call-and-poll tools behind a feature flag; a new `evals` tool group ships; and new tools create draft documents and propose document changes.
- **LLM content guardrails** — pluggable guardrails with a shadow-mode rollout path.
- **Semantic anchors** — Looker-declared anchors and incremental anchor regeneration are both enabled by default; worked examples are metric-specific; categorical conditional sums are treated as metric anchors; extraction no longer drops metrics that share a key.
- **Metadata Gap Reporting Tool** — new AI settings toggle.
- **Agent tasks from metadata changes** — a task that runs on metadata changes now picks what to watch from a single "What changed" list. See **Breaking Changes**.

#### Platform

- **OpenSearch 3.x** — supported via a unified OpenSearch client shim.
- **Deterministic read-after-write in search** — a new Elasticsearch sync controller makes read-after-write behavior deterministic.
- **Metrics and semantic models in global search** — both entity types are now searchable.
- **Metric lineage** — consumer-owned Metric downstream lineage (on by default) and column-level lineage modelled on metric upstreams, plus grouping in the metrics sidebar.
- **Search V3** — search groups and vector support; V3 documents now key on a hash of the entity URN. See **Breaking Changes**.
- **Native semantic content** — an MCL hook maintains semantic content for bridge-covered entities.
- **Search clients resolved via `OperationContext`** — search client resolution moves onto the operation context.
- **Structured properties in search** — oversized structured property values can optionally be omitted from search documents.
- **GraphQL worker pool** — pinned to 8-core defaults, with optional processor-scaled concurrency and a configurable queue size.
- **AWS IRSA** — GMS owns a single process-wide default credentials provider, constructed and closed once per process.
- **Timeseries read authorization** — dataset profile, usage and operations timeseries now enforce the matching View privileges on Rest.li and OpenAPI as well as GraphQL. See **Breaking Changes**.
- **Support login** — `ticket_id` is required when using sudo on support login.
- **SPARQL endpoint** — unanchored closures, built-in predicate IRIs, default prefixes and friendlier error messages.
- **Configuration** — per-operation configuration reads via config enrichment, and per-tenant configuration resolved from the config service.
- **Multi-tenancy flags split** — propagation, database and search flags are now independent.
- **Observability** — Context Intelligence LLM-call events record the originating job, and new Micrometer metrics cover form-assignment scroll and ingestion rollback.
- **Access tokens** — never-expiring tokens are disabled by default and allowed durations are configurable. See **Breaking Changes**.

#### Ingestion

**New ingestion sources:**

- **IBM/HCL Informix** — new connector.
- **Monte Carlo** — new connector.

**Connector improvements:**

- **BigQuery** — support for BigQuery Sharing linked datasets and multi-column partition info; the profiler moves into its own package with added SQL and identifier guards.
- **Cube** — Cube views are ingested as Semantic Models.
- **Dataplex** — supports the Dataplex Metadata Export API extraction method.
- **Metabase** — column-level lineage, model ingestion, collection tags and containers, plus general hardening.
- **MicroStrategy** — project schema is ingested as a Semantic Model.
- **Power BI** — resolves BigQuery `EXTERNAL_QUERY` federation lineage.
- **SAP Analytics Cloud** — column-level lineage to SAP Datasphere for DWC live models, and SAC → SAP Datasphere live-model lineage.
- **SAP Datasphere** — calculated column formulas are surfaced in the column description.
- **Sigma** — Sigma Dataset lineage is now recovered from Sigma's connection metadata rather than SQL. See **Breaking Changes**.
- **Kafka Connect** — SQL Server is treated as a three-level platform. See **Breaking Changes**.

**Ingestion infrastructure:**

- **Connector support statuses retiered** as Alpha / Beta / GA.
- **OAuth for the GraphQL API and circuit breakers** in the Python SDK. See **Breaking Changes**.
- **Configurable SQL lineage timeout.**
- **Lineage URN casing** is resolved against any stored casing.
- **View definitions** — table-level fallback and a self-loop guard.
- **Structured properties settings** can be ingested from the Python SDK.
- **Secret masking** — stdin envelope secrets are registered on receipt.

**Executor:**

- **`DATAHUB_EXECUTOR_TASK_MAX_RUNTIME_SECONDS` is now enforced.** See **Breaking Changes**.
- **CVE gating** — the executor image gates and tracks CVEs, with decisions sourced from the security board.
- **Ingestion Agent chat sidebar** on the Data Sources page.

#### Breaking Changes

- **Agent tasks / metadata-change triggers** — a task that runs on metadata changes now picks what to watch from a single "What changed" list ("Tag added", "Documentation changed", "Column removed") instead of choosing a change type and a change action separately. The old pairing allowed combinations that never occur, which looked like working triggers but never ran. **Action:** none. Existing tasks keep firing on exactly the changes they did before; filters stored in the old shape are translated on read, and the first save rewrites a task into the new shape. If you adopt the new asset lifecycle events ("Asset created", "Asset deleted", "Asset restored"), note they fire once per asset and the run-frequency limit is per asset — an ingestion run adding a thousand tables can start a thousand runs. Limit those triggers by asset type or to specific assets. For direct API callers, `TaskEventFilter`'s `categories` and `operations` lists are replaced by one `changes` list of `{ category, operation }` pairs; the old fields remain as deprecated, derived values so an older remote executor keeps reading a working filter.

- **Control-plane API key moves to a header** — GMS now sends the control-plane API key in an `X-Control-Plane-Api-Key` request header instead of an `api_key` URL query parameter, so the credential no longer reaches proxies, CDNs, gateways or GMS's own logs. **Action:** deploy a control plane that accepts both the header and the legacy query parameter _before_ this GMS version, then drop query-parameter support once older GMS versions have aged out. A control plane that only reads `api_key` returns non-200 for every lookup, and trial-expiration reporting in `appConfig` stops. **Also rotate the key** — any instance that ran the previous behavior may have it in historical proxy, CDN and GMS logs.

- **LLM token usage is reported to Metronome as `token-billing` events** — `event_type` changes from `llm_tokens` to `token-billing`, `model_name` splits into `model` and `provider`, `cache_read`/`cache_write` become `cached_input_tokens`/`cached_write_tokens`, `credits_micro` is removed (Metronome prices the call), and a `urn` identifies the conversation, eval run, job or task that spent the tokens. **Action:** rebuild any Metronome billable metric, product or dashboard that reads the old event type or property names — they receive no data after the upgrade. Rates are keyed on `model` + `provider`, so every model/platform pair needs a rate. Expect a gap in billed LLM tokens during the deploy window itself; it clears once every component is on the new version. Nothing outside Metronome changes.

- **MCP server: the `?token=` query parameter no longer works** — every MCP endpoint takes the access token in the `Authorization: Bearer <token>` header only, and a request with a `token` query parameter gets a `400 Bad Request` explaining the header form. A token in a URL persists in proxy and CDN logs, browser history and `Referer` headers. **Action:** point MCP clients at the plain URL and send the header. Clients that cannot set headers can use `mcp-remote` with `--header "Authorization: Bearer <token>"`.

- **`DATAHUB_EXECUTOR_TASK_MAX_RUNTIME_SECONDS` is now enforced** — previously accepted and ignored. **Action:** re-check any value already set in your executor configuration before upgrading, since it will start terminating long-running tasks.

- **Metric upstreams and lineage privileges** — charts, dashboards and datasets may take a Metric as an upstream, stored on the shared `upstreamMetrics` aspect. OpenAPI and Rest.li writes of `upstreamMetrics` require **Edit Lineage** or **Edit Entity** on the consumer and on each destination Metric. A dataset cannot both consume a Metric via `upstreamMetrics` and appear on that Metric's `metricUpstreams.datasetUpstreams`. **Action:** automation that sent unsupported `updateLineage` pairs now receives an error instead of a silent no-op. Use `metricUpstreams` to attach a dataset to a Metric, and grant **Edit Lineage** to any client writing `upstreamMetrics` outside the UI.

- **Sigma ingestion: dataset lineage route changed** ([#19815](https://github.com/datahub-project/datahub/pull/19815)) — Sigma ended dataset-as-data-source support on 2026-09-15 and dataset-backed workbook elements no longer return SQL, so Sigma Dataset lineage URNs are built from `connection_to_platform_map` rather than `chart_sources_platform_mapping`. `env` and `platform_instance` now come from the recipe unless a mapping entry supplies them, adding an entry makes its `env` authoritative (defaulting to `PROD`), and identifier casing changes on platforms other than Snowflake. **Action:** add the Sigma `connectionId` to `connection_to_platform_map` with the `env` and `platform_instance` your warehouse connector used, plus `convert_urns_to_lowercase: true` if you want the previous spelling for those table names. The connector warns when a mapping's `env` or `platform_instance` no longer applies.

- **Search V3 document IDs are hashed** — Search V3 documents now use a SHA-256 hex of the entity URN as `_id` instead of the URL-encoded URN; the full URN remains available as the `urn` field. V2 indices are unchanged. **Action:** none if Search V3 write was never enabled. If an environment already wrote V3 documents, run RestoreIndices with `-a clean` (or reindex into a fresh index) — a fill or reindex without clean leaves the old `_id` in place and updates create duplicates.

- **Structured property hard-delete requires a soft-delete first** ([#19445](https://github.com/datahub-project/datahub/pull/19445)) — hard-deleting an active structured property, or deleting its `propertyDefinition` aspect directly, is now rejected across the UI, GraphQL, OpenAPI, Rest.li and the CLI. Hard deletion left the property's qualified name in the search index mappings, so the name could not be reused until affected indices were reindexed. The UI delete flow performs both steps automatically. **Action:** where automation issued a single hard delete, soft-delete then hard-delete. To reuse a name burned by an earlier hard delete, run SystemUpdate with `ELASTICSEARCH_INDEX_BUILDER_MAPPINGS_REINDEX=true`.

- **Structured properties search indexing is no longer dynamic** ([#19490](https://github.com/datahub-project/datahub/pull/19490)) — the `structuredProperties` object is mapped `dynamic: false`, and creating a property definition adds an explicit field mapping to each declared entity type's index. Dynamic mapping could lock a wrong `text` mapping and break filters and facets. Existing indexes keep their mappings until reindexed. **Action:** none for standard deployments. If you run with `ENABLE_STRUCTURED_PROPERTIES_HOOK=false`, values on newly created indexes are no longer searchable at all — re-enable the hook and re-save affected definitions, or run SystemUpdate with `ENABLE_STRUCTURED_PROPERTIES_SYSTEM_UPDATE=true`.

- **Timeseries read authorization** — dataset profile, usage and operations timeseries (and dashboard/chart usage statistics) require the matching **View Dataset Profile / Usage / Operations** privileges on Rest.li and OpenAPI as well as GraphQL. Dedicated timeseries APIs also require **Get Timeseries Aspect API** when REST API authorization is enabled. Single-aspect GET/HEAD returns 403 without the privilege; assembled entity GET omits the aspect. **Action:** grant the View Dataset Profile / Usage / Operations privileges, or rely on the default `view-dataset-sensitive` policy, for any API client that previously read these aspects with only entity GET.

- **Role and group membership writes are authorized at the aspect layer** — adding a role to a user or group (`roleMembership`) requires **Manage Policies**; adding a user to a group requires **Edit Group Members**; adding a corpGroup owner requires **Edit Owners**. When the acting user is the one being added, both require **Manage Users & Groups**. Only additions are checked. Previously **Edit Entity** on a user was enough to grant that user the Admin role. **Action:** grant **Manage Policies** to automation that writes `roleMembership` outside the UI. Group-membership sync (LDAP, Okta, Azure AD, `datahub user upsert`) needs **Edit Group Members** or **Manage Users & Groups**, not Manage Policies. Group owners can no longer add themselves to groups they own.

- **Analytics API requires its own privilege** — `POST /openapi/v2/analytics/datahub_usage_events/_search` now requires **Analytics API access** or **Manage System Operations**. It previously accepted **View Analytics**, which every user holds by default, even though the endpoint forwards a caller-supplied search request to the search engine. **Action:** grant **Analytics API access** to any service account or script calling this endpoint. The in-app analytics dashboard still works with **View Analytics**.

- **Compliance form assignment requires Manage Compliance Forms** — `batchAssignForm`, `batchRemoveForm` and `createDynamicFormAssignment`, and any write to a form's `forms` or `dynamicFormAssignment` aspect, now require the **Manage Compliance Forms** platform privilege. Writes that only change prompt completion or verification state are unaffected, so assignees can still complete forms. These mutations previously performed no authorization check. **Action:** grant **Manage Compliance Forms** to automation that assigns or unassigns forms outside the UI.

- **Asset summary settings writes are authorized** ([#19745](https://github.com/datahub-project/datahub/pull/19745)) — `updateAssetSettings` and any write to the `assetSettings` aspect require **Edit Entity** or **Manage Asset Summary** on the target asset. The GraphQL mutation previously performed no authorization check, so any authenticated user could change any asset's summary template. **Action:** grant **Manage Asset Summary** or **Edit Entity** to automation writing `assetSettings` outside the UI. Note that OpenAPI and Rest.li still authorize this as a generic entity update, so **Manage Asset Summary** alone is not sufficient there.

- **GMS refuses to start with both UI indexing paths disabled** ([#19295](https://github.com/datahub-project/datahub/pull/19295)) — when GMS consumes MCLs and both `PRE_PROCESS_HOOKS_UI_ENABLED=false` and `PRE_PROCESS_HOOKS_REPROCESS_ENABLED` are unset or false, startup fails. That combination stored UI-created entities without writing search documents, so ingestion sources and execution requests never appeared. **Action:** leave `PRE_PROCESS_HOOKS_UI_ENABLED` at its default `true`, or set `PRE_PROCESS_HOOKS_REPROCESS_ENABLED=true`. Set the same pair on GMS and MAE. Helm charts that set both MAE env vars to false while GMS preprocess was on will fail MAE startup after this version.

- **Never-expiring access tokens are disabled by default** — allowed finite durations are configured as a comma-separated ISO-8601 list (`ACCESS_TOKEN_ALLOWED_DURATIONS`, default `PT1H,P1D,P7D,P30D,P90D,P180D,P365D`). Exactly one of GraphQL `duration` or `durationIso` must be provided on create. **Already-issued tokens, including never-expiring ones, keep working** — this is create-time policy only. **Action:** set `ACCESS_TOKEN_ALLOW_NO_EXPIRY=true` to allow never-expire tokens again, or set `ACCESS_TOKEN_ALLOWED_DURATIONS` to customize available TTLs.

- **pgQueue idle-poll configuration replaced** ([#19673](https://github.com/datahub-project/datahub/pull/19673)) — idle consumers now back off exponentially (1s doubling to 5s). `DATAHUB_PGQUEUE_EMPTY_POLL_SLEEP_MS` and `DATAHUB_PGQUEUE_MCL_EMPTY_POLL_SLEEP_MS` are removed and are **not** read as aliases; use `DATAHUB_PGQUEUE_EMPTY_POLL_SLEEP_MIN_MS` plus the per-topic ceilings. On the Python actions pgQueue event source, `poll_interval_seconds` is removed in favor of `empty_poll_sleep_min_millis` / `empty_poll_sleep_max_millis`. **Action:** delete the old env keys and recipe field.

- **pgQueue maintenance interval must map to a constant cron cadence** ([#19673](https://github.com/datahub-project/datahub/pull/19673)) — `postgres.pgQueue.maintenance.intervalSeconds` must be a minute interval dividing 60, an hour interval dividing 24, or exactly one day. Values such as 5 hours or 7 minutes now fail setup. **Action:** use 1, 2, 3, 4, 5, 6, 10, 12, 15, 20 or 30 minutes; 1, 2, 3, 4, 6, 8 or 12 hours; or 1 day.

- **`KAFKA_CONSUMER_PERCENTILES` removed** ([#19549](https://github.com/datahub-project/datahub/pull/19549)) — client-side percentile gauges for `messaging_queue_time_seconds` are no longer configured; Micrometer already ignored them when a histogram is enabled, so `quantile=` series were not exported. Histogram bucket and count series are unchanged. **Action:** delete the variable, and switch any dashboard or alert querying a `quantile` label to `histogram_quantile()` over `messaging_queue_time_seconds_bucket`.

- **OpenAPI entity create authorization is per-URN** ([#18944](https://github.com/datahub-project/datahub/pull/18944)) — `createEntity` / `createGenericEntities` and aligned batch paths authorize each URN after building the MCP batch, using entity existence to choose Create vs Edit privileges. A principal with only **Create Entity** can no longer overwrite an existing entity via create/UPSERT. **Action:** grant **Edit Entity** to callers that update existing entities or use default `createAspect`; keep create-only policies for true creates.

- **Domain-scoped create and domain writes** ([#18944](https://github.com/datahub-project/datahub/pull/18944)) — ingest-time authorization evaluates domain-scoped **Create Entity** / **Edit Entity** policies against _proposed_ `domains` when establishing domains on a new entity. A `domains` PATCH always requires **Edit Entity**, and domain-scoped Edit must allow both the before and after domain membership. **Action:** domain-separated writers must include matching `domains` in the create; grant Edit on both source and destination domains before moving assets between domains with a domains PATCH.

- **Group member removal now removes unmigrated members** ([#18982](https://github.com/datahub-project/datahub/pull/18982)) — `removeGroupMembers` removes members holding only the legacy `groupMembership` aspect. Previously the mutation stripped only `nativeGroupMembership`, reported success, and left the user in the group with access intact. **Action:** none required.

- **MySQL, MariaDB, Doris and TiDB profiling limits are enforced** ([#18699](https://github.com/datahub-project/datahub/pull/18699)) — `profiling.profile_table_row_limit` and `profiling.profile_table_size_limit` were previously accepted and ignored on these sources. **Action:** re-check any value already set in your recipe before upgrading, as it will start filtering profiles. Both default to `null` on these sources, so nothing changes unless you set them.

- **`sql-queries` reports total failure** ([#19099](https://github.com/datahub-project/datahub/pull/19099)) — a run where every input line is unparseable or every query fails now reports failure and exits non-zero instead of completing successfully with nothing to show. Individual bad rows are still skipped and counted. `enable_lazy_schema_loading` is removed, and two report fields are renamed: `num_queries_processed_sequential` → `num_queries_processed`, `num_temp_tables_detected` → `num_temp_table_matches`. **Action:** remove `enable_lazy_schema_loading` from your recipe.

- **Python SDK circuit breakers move to `DataHubGraph`** ([#18548](https://github.com/datahub-project/datahub/pull/18548)) — `BaseApi` and its `Assertion` / `Operation` subclasses query GMS through `DataHubGraph` instead of a `gql` client, gaining OAuth, PAT, system auth and mTLS. The `transport=` constructor argument is replaced by `graph=`, `.client` by `.graph`, and GraphQL errors raise `GraphError` instead of `TransportQueryError`. **Action:** update constructor calls and error handling.

- **Kafka Connect treats SQL Server as three-level** — JDBC sink connectors default the schema to `dbo` when `schema.name` is unset, so sink URNs change from `database.table` to `database.dbo.table`. JDBC source connectors drop a dataset with a warning when the schema cannot be resolved, rather than emitting a two-level URN. Oracle JDBC sinks now emit `schema.table`. **Action:** none if your SQL Server assets were ingested with schema in the URN. If a source connector starts warning about a missing schema, set `schema.name` on the connector or use `generic_connectors` to force the mapping.

- **Elasticsearch ingestion requires `opensearch-py` 3.x** — `acryl-datahub[elasticsearch]` now requires `opensearch-py>=3.0.0,<4.0.0`. The 3.x client still talks to OpenSearch 2 clusters. **Action:** none if you install via the extra; if you pin `opensearch-py` yourself, raise the floor.

- **The legacy Great Expectations SQL profiler is removed** — the SQLAlchemy profiler, default since v1.1.0 and at feature parity, is now the only SQL profiler. The `profiling.method` option and the `acryl-datahub[profiling-ge]` extra no longer exist; recipes that still set `profiling.method` emit a deprecation warning and profile with SQLAlchemy. For Unity Catalog, `profiling.method: ge` is rejected. **Action:** remove `profiling.method` from recipes and drop the `profiling-ge` extra. This does not affect the separate Great Expectations integration that ingests GX validation results into DataHub.

#### Deprecations

- **The operational dashboard under `/admin/dashboard` is being removed.** It proxied an internal Grafana dashboard and has no equivalent after the migration to a new observability stack. Operational signals remain available through the DataHub public APIs, with published SLOs as the forward-looking replacement. Behavior is controlled per instance by `GRAFANA_DASHBOARD_MODE`:

  | Release | Mode        | What customers see                                               |
  | ------- | ----------- | ---------------------------------------------------------------- |
  | 2.2     | `TOMBSTONE` | `410 Gone` with a deprecation notice pointing at the public APIs |
  | 2.3.0   | `DISABLED`  | `404` — the path no longer responds                              |
  | 2.4.0   | —           | the proxy and its configuration are deleted                      |

  The default remains `ENABLED` in this release, so nothing changes until an instance is flipped.

- **The `generate_sql_sketch`, `preview_sql_context` and `save_sql_context` MCP tools are removed.** All three were cloud-only and default-off, each gated behind its own environment variable. Those gates, the `SQL_SKETCH_SYNTHESIS_MODEL` override, and the privilege gating that existed only to guard `save_sql_context` are gone. Deployments that never enabled the gates are unaffected, and the retrieval-only `find_sql_context` and `draft_sql_for_tables` tools are unchanged.

- **`TaskEventFilter.categories` and `.operations`** are deprecated in favor of a single `changes` list. They remain readable, derived from `changes`, so an older remote executor keeps working; they will be removed once every executor reads `changes`.

- **Legacy per-observation proposals** are cleaned up automatically on upgrade.

#### Security / Dependencies

- **transformers**: bumped to 5.17.0 to address CVE-2026-9856.
- **Apache Parquet**: bumped to 1.18.1 to address CVE-2026-73334.
- **mariadb-java-client**: bumped to 2.7.14 to address CVE-2026-55856, CVE-2026-55857 and CVE-2026-55858.
- **cryptography**: bumped to 50.0.1 in the integrations service to address CVE-2026-69247.
- **cryptography / pyOpenSSL**: ingestion floors raised to address CVE-2026-69247 and CVE-2026-27459.
- **Spring Framework / Boot / Security**: bumped to 7.0.9, 4.0.8 and 7.0.7 respectively.
- **httpclient5, Spring Boot and the OpenTelemetry agent**: bumped for reported CVEs.
- **nltk**: bumped 3.10.0 → 3.10.3.
- **gitpython**: bumped to 3.1.61 in the Cloud image lockfiles.
- **setuptools**: the Python package layer is refreshed in the executor and actions images to address CVE-2025-47273.
- **curl**: base image packages refreshed to address reported curl CVEs.
- **mcp and json-repair**: bumped for security fixes.
- **Executor CVE gating**: the executor image now gates and tracks CVEs as part of the build.

#### Bug Fixes

- Restored searchability of structured property values on newly created indexes by adding explicit field mappings at definition time.
- Fixed silent no-ops on unsupported `updateLineage` pairs, which now return an error.
- Fixed group member removal leaving unmigrated members in the group with access intact.
- Prevented GMS from starting in a configuration that stored UI-created entities without writing search documents.
- Fixed manual semantic-search reindexes being cancelled by the scheduled run.
- Fixed semantic-anchor extraction dropping metrics that share a key.
- Fixed a Metronome billing gap where ledger state could run ahead of the archive, with an automatic publish repair.

## Known Issues

- None known at release time.

#### Environment Variables

| Variable                                           | Default                              | Notes                                                                                                                                                     |
| -------------------------------------------------- | ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SPARQL_API_ENABLED`                               | `false`                              | Enables the SPARQL API. Now also gates the Ontology Explorer's SPARQL tab, so enabling it for programmatic access also exposes the query editor to users. |
| `DATAHUB_EXECUTOR_TASK_ENTITY_TRIGGERS_ENABLED`    | `false`                              | Enables metadata-change triggers for agent tasks.                                                                                                         |
| `GRAPHQL_CONCURRENCY_SCALE_WITH_PROCESSORS`        | `false`                              | Scales the GraphQL worker pool with available processors.                                                                                                 |
| `GRAPHQL_CONCURRENCY_QUEUE_SIZE`                   | `0`                                  | GraphQL worker queue size.                                                                                                                                |
| `NATIVE_SEMANTIC_CONTENT_SYSTEMIC_COOL_DOWN_MS`    | `30000`                              | Cool-down for the native semantic content hook.                                                                                                           |
| `AGENTS_AS_TOOLS_ASYNC`                            | `false`                              | Serves agents as call-and-poll MCP tools.                                                                                                                 |
| `RELATIONSHIP_GRAPH_TOOLS_ENABLED`                 | `true`                               | Enables relationship-graph MCP tools.                                                                                                                     |
| `CHATBOT_SKILLS_ENABLED`                           | `true`                               | Enables chatbot skills.                                                                                                                                   |
| `OAUTH2_DCR_EVICT_IDLE_DAYS`                       | `7`                                  | Dynamic client registration eviction window.                                                                                                              |
| `OAUTH2_DCR_EVICT_THRESHOLD_PERCENT`               | `90`                                 | Eviction threshold.                                                                                                                                       |
| `OAUTH2_DCR_EVICT_BATCH_SIZE`                      | `100`                                | Eviction batch size.                                                                                                                                      |
| `EXTERNAL_OAUTH_MAP_TO_CORPUSER`                   | `false`                              | Opt-in mapping of external OAuth identities to corp users.                                                                                                |
| `AI_AGENT_PLUGIN_OPT_IN_URNS`                      | _(empty)_                            | Opt-in list of agent plugin URNs.                                                                                                                         |
| `DATAHUB_LLM_PROVIDER`                             | —                                    | Default LLM provider.                                                                                                                                     |
| `DATAHUB_LLM_READ_TIMEOUT_SECONDS`                 | `60`                                 | LLM read timeout.                                                                                                                                         |
| `DATAHUB_LLM_CONNECT_TIMEOUT_SECONDS`              | `60`                                 | LLM connect timeout.                                                                                                                                      |
| `GENAI_CREDITS_PER_USD`                            | `1`                                  | AI credit conversion rate.                                                                                                                                |
| `DATAHUB_BI_GROUNDING_ENABLED`                     | `true`                               | Kill switch for BI grounding.                                                                                                                             |
| `BEDROCK_EMBEDDING_DIMENSIONS`                     | `0`                                  | Bedrock embedding dimensionality.                                                                                                                         |
| `ES_BULK_DEFAULT_TIMEOUT_SECONDS`                  | `60`                                 | Default Elasticsearch bulk timeout.                                                                                                                       |
| `SEARCH_STRUCTURED_PROPERTY_RANK_FEATURES_ENABLED` | `false`                              | Enables structured property rank features in search.                                                                                                      |
| `OPTIMISTIC_LOCKING_ENABLED`                       | `false`                              | Enables optimistic locking on writes.                                                                                                                     |
| `SHOW_OBSERVATION_CLUSTER_PROPOSALS`               | `false`                              | Shows observation cluster proposals.                                                                                                                      |
| `GRAFANA_DASHBOARD_MODE`                           | `ENABLED`                            | Controls `/admin/dashboard`. See **Deprecations**.                                                                                                        |
| `GRAFANA_DASHBOARD_REMOVAL_RELEASE`                | —                                    | Release named in the deprecation notice.                                                                                                                  |
| `GRAFANA_DASHBOARD_SUNSET_DATE`                    | —                                    | Date named in the deprecation notice.                                                                                                                     |
| `GRAFANA_DASHBOARD_DEPRECATION_URL`                | —                                    | Link in the deprecation notice.                                                                                                                           |
| `NOTIFICATION_LOGO_URL`                            | —                                    | Logo used in notifications.                                                                                                                               |
| `NOTIFICATION_FOOTER_TEXT`                         | —                                    | Footer text used in notifications.                                                                                                                        |
| `LOG_AGGREGATOR_ENDPOINT`                          | —                                    | Opt-in log aggregation endpoint.                                                                                                                          |
| `UVICORN_TIMEOUT_KEEP_ALIVE`                       | `75`                                 | Integrations service keep-alive timeout.                                                                                                                  |
| `ACCESS_TOKEN_ALLOWED_DURATIONS`                   | `PT1H,P1D,P7D,P30D,P90D,P180D,P365D` | Allowed access-token TTLs. See **Breaking Changes**.                                                                                                      |
| `ACCESS_TOKEN_ALLOW_NO_EXPIRY`                     | `false`                              | Allows never-expiring access tokens.                                                                                                                      |
| `DATAHUB_PGQUEUE_EMPTY_POLL_SLEEP_MIN_MS`          | —                                    | Idle pgQueue poll floor. See **Breaking Changes**.                                                                                                        |

**Removed:** `DATAHUB_PGQUEUE_EMPTY_POLL_SLEEP_MS`, `DATAHUB_PGQUEUE_MCL_EMPTY_POLL_SLEEP_MS`, `KAFKA_CONSUMER_PERCENTILES`, `GENERATE_SQL_SKETCH_TOOL`, `SAVE_SQL_CONTEXT_TOOL`, `SQL_SKETCH_SYNTHESIS_MODEL`.
