import Route from '@ember/routing/route';

/**
 * Route to catch this use case:
 *
 * /entityType/
 *
 * This will yield to page not found as this is not a valid page (urn required)
 */
export default class EntityTypeIndex extends Route {}
