{{#with this.data as |data|}}
  <NachoTable::NachoTable
    @class={{@options.className}}
    @data={{data}}
    @tableConfigs={{this.tableConfigs}}
  />
{{else}}
  <EmptyState @heading={{this.emptyStateMessage}} />
{{/with}}