import { SocialAction } from '@datahub/data-models/constants/entity/person/social-actions';

/**
 * Social actions that are still in progress/development and may be flag guarded out
 */
export const pendingSocialActions: Array<SocialAction> = [SocialAction.FOLLOW];
