/**
 * Describes the interface for the configurable user settings
 */
export interface IUserSettings {
  // Whether or not the user wants to see the virtual assistant features
  isVirtualAssistantActive?: boolean;
}
