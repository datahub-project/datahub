/**
 * Set of options that we need to render a graph
 */
export interface IGraphOptions {
  /**
   * If enabled, graph will have some enhanced characteristics for lineage exploration
   */
  lineageMode: boolean;
}
