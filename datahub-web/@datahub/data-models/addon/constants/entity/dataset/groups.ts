/**
 * Regex pattern used to extract the group name and the namespace from the URN resulting from the `readDatasetGroups` api call
 *
 */
export const datasetGroupNameRegexPattern = /^urn:li:datasetGroup:\((.*)\)/;
