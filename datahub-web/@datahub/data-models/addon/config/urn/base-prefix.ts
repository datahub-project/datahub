/**
 * Base for all corpuser type user urns. A person's username is appended to this base
 */
export const corpUserUrnBasePrefix = 'urn:li:corpuser:';

/**
 * Base for all actor type user urns. A person's username is appended to this base
 */
export const actorUrnBasePrefix = 'urn:li:actor:';

/**
 * Base for all grid group type urns. A group's account name is appended to this base
 */
export const gridGroupUrnBasePrefix = 'urn:li:gridGroup:';
