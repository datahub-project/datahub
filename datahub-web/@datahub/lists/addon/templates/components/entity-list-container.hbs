{{yield
  (hash
    name=this.name
    count=this.listCount
    dataModel=this.entity
    listName=this.listName
    selectedAll=this.selectedAll
    onSelectEntity=this.onSelectEntity
    removeAllSelected=this.removeAllSelected
    onSelectEntityList=this.onSelectEntityList
    list=(readonly this.entityListWithLinkAttrs)
    hasMultipleSelected=this.hasMultipleSelected
    hydrateEntitiesTask=this.hydrateEntitiesTask
    selectedEntities=(readonly this.selectedEntities)
  )
}}
