/**
 * Number of milliseconds since midnight, January 1, 1970 UTC. It must be a positive number
 * @namespace common
 * @alias number provide a more semantic type annotation when used to represent javascript date timestamps
 */
export type Time = number;
