/**
 * Global properties as defined in the frame config
 * @export
 * @interface IFrameGlobalProperties
 */
export interface IFrameGlobalProperties {
  // Control whether Frame would ignore syntax NPE errors from MVEL expressions
  // default false
  strictMode: boolean;
}
