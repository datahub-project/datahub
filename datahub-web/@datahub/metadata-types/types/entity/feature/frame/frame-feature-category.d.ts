/**
 * Dot (.) separated feature category path, e.g. Member.Activity.Job.ApplySave
 * @alias string Provides a semantic type annotation for the string format, does not assert the type of the string
 */
export type FrameFeatureCategory = string;
