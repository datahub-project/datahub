/**
 * Properties related to a Venice source
 * @export
 * @interface IVeniceSourceProperties
 */
export interface IVeniceSourceProperties {
  // Venice database name
  storeName: string;
  // MVEL key expression
  keyExpression: string;
}
