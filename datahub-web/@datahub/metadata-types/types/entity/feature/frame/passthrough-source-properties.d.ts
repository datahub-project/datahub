/**
 * Represents an passthrough source and related properties
 * @export
 * @interface IPassthroughSourceProperties
 */
export interface IPassthroughSourceProperties {
  // Describes the class that the features will be extracted from. It can POJO or pegasus objects
  dataModel?: string;
}
