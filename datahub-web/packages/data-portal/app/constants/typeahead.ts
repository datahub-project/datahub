/**
 * Set our max number of suggestions we want to show per typeahead search result
 */
export const suggestionLimit = 10;

/**
 * Backend number of characters to return something meaninful in the suggestions
 */
export const typeaheadBackendThreshold = 3;
