<div class="col-md-3">
  {{#dynamic-link
    params=link
    class=(concat "browse-card-entity-" (dasherize title))
  }}
    <div class="browse-card-container">
      <div class="browse-card">
        <p class="browse-card__title">
          <strong>
            {{title}}
          </strong>
        </p>
        <p class="browse-card__description">
          {{#if showSpinner}}
            {{pendulum-ellipsis-animation}}
          {{else}}
            {{description}}
          {{/if}}
        </p>
      </div>
    </div>
  {{/dynamic-link}}
</div>
