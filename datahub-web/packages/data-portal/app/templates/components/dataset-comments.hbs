<Comment::CommentStream @comments={{comments}} @commentTypes={{commentTypes}} @updateCommentInStream={{action "handleStreamAction" streamActions.modify}} @deleteCommentFromStream={{action "handleStreamAction" streamActions.destroy}} @addCommentToStream={{action "handleStreamAction" streamActions.add}} as |stream|>

  {{stream.header}}

  <stream.new>
    + Add a comment...
  </stream.new>

  {{#each stream.comments as |comment|}}
    <stream.item @comment={{comment}}>
      {{comment.text}}
    </stream.item>
  {{/each}}

</Comment::CommentStream>
