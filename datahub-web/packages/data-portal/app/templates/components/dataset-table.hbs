{{yield
  (hash
    searchTerm=searchTerm
    data=(readonly data)
    sortColumnWithName=(readonly sortColumnWithName)
    sortDirection=(readonly sortDirection)
    sortBy=(readonly sortBy)
    page=(readonly page)
    limit=(readonly limit)
    pageLengths=(readonly pageLengths)
    beginOffset=(readonly beginOffset)
    endOffset=(readonly endOffset)
    onPageChanged=(action "pageDidChange")
    onLimitChanged=(action "limitDidChange")
    filterDidChange=(action "filterDidChange")
    head=(component tableHeaderComponent
      sortColumnWithName=(readonly sortColumnWithName)
      sortDirection=(readonly sortDirection)
      sortDidChange=(action "sortDidChange"))
    body=(component tableBodyComponent
      tableRowComponent=tableRowComponent)
    foot=(component tableFooterComponent)
  )
}}
