<Search::SearchMain
  @searchConfig={{this.model.searchConfig}}
  @trackPrefix={{this.model.trackPrefix}}
  @title={{this.model.title}}
  @keyword={{this.model.keyword}}
  @facets={{this.model.facets}}
  @page={{this.model.page}}
  @entity={{this.model.entity}}
/>
