package com.datahub.metadata.graphql;

// TODO: Inject this into the GraphQL engine and start enforcing some policies. We can
// also share this inside of the GMS module as well, it will just make circular API calls, w
// which should suffice for now.
public class AuthorizationManagerFactory {
}
