# Optional pgAnalytics multi-store overlay (DATAHUB_PGANALYTICS_CONFIG_FILE).
# Defaults (product store + datahub_usage routing) live in application.yaml.
# Mount a file here only to override stores/routing; omit `stores`/`routing` keys you want to keep.
postgres:
  pgAnalytics: {}
