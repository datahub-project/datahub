---
title: URNs
---
<div className="sphinx-api-docs">
<a id="module-datahub.metadata.urns"></a>

<a id="urns"></a>




### <span className="visually-hidden">ApplicationUrn</span> {#datahub.metadata.urns.ApplicationUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">ApplicationUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.ApplicationUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`ApplicationUrn`](#datahub.metadata.urns.ApplicationUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'application'`]]* *= 'application'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`ApplicationKeyClass`](models.mdx#datahub.metadata.schema_classes.ApplicationKeyClass)) – 
* **Return type:**
  [`ApplicationUrn`](#datahub.metadata.urns.ApplicationUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`ApplicationKeyClass`](models.mdx#datahub.metadata.schema_classes.ApplicationKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">AssertionUrn</span> {#datahub.metadata.urns.AssertionUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">AssertionUrn</span>(<span class="arg-name">assertion_id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.AssertionUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **assertion_id** (`Union`[[`AssertionUrn`](#datahub.metadata.urns.AssertionUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'assertion'`]]* *= 'assertion'*

#### *property* assertion_id *: str*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`AssertionKeyClass`](models.mdx#datahub.metadata.schema_classes.AssertionKeyClass)) – 
* **Return type:**
  [`AssertionUrn`](#datahub.metadata.urns.AssertionUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`AssertionKeyClass`](models.mdx#datahub.metadata.schema_classes.AssertionKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">BusinessAttributeUrn</span> {#datahub.metadata.urns.BusinessAttributeUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">BusinessAttributeUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.BusinessAttributeUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`BusinessAttributeUrn`](#datahub.metadata.urns.BusinessAttributeUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'businessAttribute'`]]* *= 'businessAttribute'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`BusinessAttributeKeyClass`](models.mdx#datahub.metadata.schema_classes.BusinessAttributeKeyClass)) – 
* **Return type:**
  [`BusinessAttributeUrn`](#datahub.metadata.urns.BusinessAttributeUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`BusinessAttributeKeyClass`](models.mdx#datahub.metadata.schema_classes.BusinessAttributeKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">ChartUrn</span> {#datahub.metadata.urns.ChartUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">ChartUrn</span>(<span class="arg-name">dashboard_tool</span>, <span class="arg-name">chart_id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.ChartUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **dashboard_tool** (`str`)
  * **chart_id** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'chart'`]]* *= 'chart'*

#### *property* chart_id *: str*

#### *classmethod* create_from_ids(platform, name, platform_instance=None)

* **Parameters:**
  * **platform** (`str`)
  * **name** (`str`)
  * **platform_instance** (`Optional`[`str`])
* **Return type:**
  [`ChartUrn`](#datahub.metadata.urns.ChartUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* dashboard_tool *: str*

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`ChartKeyClass`](models.mdx#datahub.metadata.schema_classes.ChartKeyClass)) – 
* **Return type:**
  [`ChartUrn`](#datahub.metadata.urns.ChartUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`ChartKeyClass`](models.mdx#datahub.metadata.schema_classes.ChartKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">ContainerUrn</span> {#datahub.metadata.urns.ContainerUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">ContainerUrn</span>(<span class="arg-name">guid</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.ContainerUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **guid** (`Union`[[`ContainerUrn`](#datahub.metadata.urns.ContainerUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'container'`]]* *= 'container'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`ContainerKeyClass`](models.mdx#datahub.metadata.schema_classes.ContainerKeyClass)) – 
* **Return type:**
  [`ContainerUrn`](#datahub.metadata.urns.ContainerUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* guid *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`ContainerKeyClass`](models.mdx#datahub.metadata.schema_classes.ContainerKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">CorpGroupUrn</span> {#datahub.metadata.urns.CorpGroupUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">CorpGroupUrn</span>(<span class="arg-name">name</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.CorpGroupUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **name** (`Union`[[`CorpGroupUrn`](#datahub.metadata.urns.CorpGroupUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'corpGroup'`]]* *= 'corpGroup'*

#### *classmethod* create_from_id(id)

#### Deprecated
Deprecated since version 0.12.0.2: Use the constructor instead

* **Parameters:**
  **id** (`str`)
* **Return type:**
  [`CorpGroupUrn`](#datahub.metadata.urns.CorpGroupUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`CorpGroupKeyClass`](models.mdx#datahub.metadata.schema_classes.CorpGroupKeyClass)) – 
* **Return type:**
  [`CorpGroupUrn`](#datahub.metadata.urns.CorpGroupUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### to_key_aspect()

* **Return type:**
  [`CorpGroupKeyClass`](models.mdx#datahub.metadata.schema_classes.CorpGroupKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">CorpUserUrn</span> {#datahub.metadata.urns.CorpUserUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">CorpUserUrn</span>(<span class="arg-name">username</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.CorpUserUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **username** (`Union`[[`CorpUserUrn`](#datahub.metadata.urns.CorpUserUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'corpuser'`]]* *= 'corpuser'*

#### *classmethod* create_from_id(id)

#### Deprecated
Deprecated since version 0.12.0.2: Use the constructor instead

* **Parameters:**
  **id** (`str`)
* **Return type:**
  [`CorpUserUrn`](#datahub.metadata.urns.CorpUserUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`CorpUserKeyClass`](models.mdx#datahub.metadata.schema_classes.CorpUserKeyClass)) – 
* **Return type:**
  [`CorpUserUrn`](#datahub.metadata.urns.CorpUserUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`CorpUserKeyClass`](models.mdx#datahub.metadata.schema_classes.CorpUserKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`

#### *property* username *: str*



</div>




### <span className="visually-hidden">DashboardUrn</span> {#datahub.metadata.urns.DashboardUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DashboardUrn</span>(<span class="arg-name">dashboard_tool</span>, <span class="arg-name">dashboard_id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DashboardUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **dashboard_tool** (`str`)
  * **dashboard_id** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dashboard'`]]* *= 'dashboard'*

#### *classmethod* create_from_ids(platform, name, platform_instance=None)

* **Parameters:**
  * **platform** (`str`)
  * **name** (`str`)
  * **platform_instance** (`Optional`[`str`])
* **Return type:**
  [`DashboardUrn`](#datahub.metadata.urns.DashboardUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* dashboard_id *: str*

#### *property* dashboard_tool *: str*

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DashboardKeyClass`](models.mdx#datahub.metadata.schema_classes.DashboardKeyClass)) – 
* **Return type:**
  [`DashboardUrn`](#datahub.metadata.urns.DashboardUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DashboardKeyClass`](models.mdx#datahub.metadata.schema_classes.DashboardKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataContractUrn</span> {#datahub.metadata.urns.DataContractUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataContractUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataContractUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataContractUrn`](#datahub.metadata.urns.DataContractUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataContract'`]]* *= 'dataContract'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataContractKeyClass`](models.mdx#datahub.metadata.schema_classes.DataContractKeyClass)) – 
* **Return type:**
  [`DataContractUrn`](#datahub.metadata.urns.DataContractUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataContractKeyClass`](models.mdx#datahub.metadata.schema_classes.DataContractKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataFlowUrn</span> {#datahub.metadata.urns.DataFlowUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataFlowUrn</span>(<span class="arg-name">orchestrator</span>, <span class="arg-name">flow_id</span>, <span class="arg-name">cluster</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataFlowUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **orchestrator** (`str`)
  * **flow_id** (`str`)
  * **cluster** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataFlow'`]]* *= 'dataFlow'*

#### *property* cluster *: str*

#### *classmethod* create_from_ids(orchestrator, flow_id, env, platform_instance=None)

* **Parameters:**
  * **orchestrator** (`str`)
  * **flow_id** (`str`)
  * **env** (`str`)
  * **platform_instance** (`Optional`[`str`])
* **Return type:**
  [`DataFlowUrn`](#datahub.metadata.urns.DataFlowUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *property* flow_id *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataFlowKeyClass`](models.mdx#datahub.metadata.schema_classes.DataFlowKeyClass)) – 
* **Return type:**
  [`DataFlowUrn`](#datahub.metadata.urns.DataFlowUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### get_env()

#### Deprecated
Deprecated since version 0.12.0.2: Use .cluster instead

* **Return type:**
  `str`

#### get_flow_id()

#### Deprecated
Deprecated since version 0.12.0.2: Use .flow_id instead

* **Return type:**
  `str`

#### get_orchestrator_name()

#### Deprecated
Deprecated since version 0.12.0.2: Use .orchestrator instead

* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* orchestrator *: str*

#### to_key_aspect()

* **Return type:**
  [`DataFlowKeyClass`](models.mdx#datahub.metadata.schema_classes.DataFlowKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubAccessTokenUrn</span> {#datahub.metadata.urns.DataHubAccessTokenUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubAccessTokenUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubAccessTokenUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubAccessTokenUrn`](#datahub.metadata.urns.DataHubAccessTokenUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubAccessToken'`]]* *= 'dataHubAccessToken'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubAccessTokenKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubAccessTokenKeyClass)) – 
* **Return type:**
  [`DataHubAccessTokenUrn`](#datahub.metadata.urns.DataHubAccessTokenUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubAccessTokenKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubAccessTokenKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubActionUrn</span> {#datahub.metadata.urns.DataHubActionUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubActionUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubActionUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubActionUrn`](#datahub.metadata.urns.DataHubActionUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubAction'`]]* *= 'dataHubAction'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubActionKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubActionKeyClass)) – 
* **Return type:**
  [`DataHubActionUrn`](#datahub.metadata.urns.DataHubActionUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubActionKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubActionKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubConnectionUrn</span> {#datahub.metadata.urns.DataHubConnectionUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubConnectionUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubConnectionUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubConnectionUrn`](#datahub.metadata.urns.DataHubConnectionUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubConnection'`]]* *= 'dataHubConnection'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubConnectionKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubConnectionKeyClass)) – 
* **Return type:**
  [`DataHubConnectionUrn`](#datahub.metadata.urns.DataHubConnectionUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubConnectionKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubConnectionKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubExecutionRequestUrn</span> {#datahub.metadata.urns.DataHubExecutionRequestUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubExecutionRequestUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubExecutionRequestUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubExecutionRequestUrn`](#datahub.metadata.urns.DataHubExecutionRequestUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubExecutionRequest'`]]* *= 'dataHubExecutionRequest'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`ExecutionRequestKeyClass`](models.mdx#datahub.metadata.schema_classes.ExecutionRequestKeyClass)) – 
* **Return type:**
  [`DataHubExecutionRequestUrn`](#datahub.metadata.urns.DataHubExecutionRequestUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`ExecutionRequestKeyClass`](models.mdx#datahub.metadata.schema_classes.ExecutionRequestKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubIngestionSourceUrn</span> {#datahub.metadata.urns.DataHubIngestionSourceUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubIngestionSourceUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubIngestionSourceUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubIngestionSourceUrn`](#datahub.metadata.urns.DataHubIngestionSourceUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubIngestionSource'`]]* *= 'dataHubIngestionSource'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubIngestionSourceKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubIngestionSourceKeyClass)) – 
* **Return type:**
  [`DataHubIngestionSourceUrn`](#datahub.metadata.urns.DataHubIngestionSourceUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubIngestionSourceKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubIngestionSourceKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubOpenAPISchemaUrn</span> {#datahub.metadata.urns.DataHubOpenAPISchemaUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubOpenAPISchemaUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubOpenAPISchemaUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubOpenAPISchemaUrn`](#datahub.metadata.urns.DataHubOpenAPISchemaUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubOpenAPISchema'`]]* *= 'dataHubOpenAPISchema'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubOpenAPISchemaKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubOpenAPISchemaKeyClass)) – 
* **Return type:**
  [`DataHubOpenAPISchemaUrn`](#datahub.metadata.urns.DataHubOpenAPISchemaUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubOpenAPISchemaKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubOpenAPISchemaKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubPageModuleUrn</span> {#datahub.metadata.urns.DataHubPageModuleUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubPageModuleUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubPageModuleUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubPageModuleUrn`](#datahub.metadata.urns.DataHubPageModuleUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubPageModule'`]]* *= 'dataHubPageModule'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubPageModuleKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubPageModuleKeyClass)) – 
* **Return type:**
  [`DataHubPageModuleUrn`](#datahub.metadata.urns.DataHubPageModuleUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubPageModuleKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubPageModuleKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubPageTemplateUrn</span> {#datahub.metadata.urns.DataHubPageTemplateUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubPageTemplateUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubPageTemplateUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubPageTemplateUrn`](#datahub.metadata.urns.DataHubPageTemplateUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubPageTemplate'`]]* *= 'dataHubPageTemplate'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubPageTemplateKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubPageTemplateKeyClass)) – 
* **Return type:**
  [`DataHubPageTemplateUrn`](#datahub.metadata.urns.DataHubPageTemplateUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubPageTemplateKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubPageTemplateKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubPersonaUrn</span> {#datahub.metadata.urns.DataHubPersonaUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubPersonaUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubPersonaUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubPersonaUrn`](#datahub.metadata.urns.DataHubPersonaUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubPersona'`]]* *= 'dataHubPersona'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubPersonaKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubPersonaKeyClass)) – 
* **Return type:**
  [`DataHubPersonaUrn`](#datahub.metadata.urns.DataHubPersonaUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubPersonaKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubPersonaKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubPolicyUrn</span> {#datahub.metadata.urns.DataHubPolicyUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubPolicyUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubPolicyUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubPolicyUrn`](#datahub.metadata.urns.DataHubPolicyUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubPolicy'`]]* *= 'dataHubPolicy'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubPolicyKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubPolicyKeyClass)) – 
* **Return type:**
  [`DataHubPolicyUrn`](#datahub.metadata.urns.DataHubPolicyUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubPolicyKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubPolicyKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubRetentionUrn</span> {#datahub.metadata.urns.DataHubRetentionUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubRetentionUrn</span>(<span class="arg-name">entity_name</span>, <span class="arg-name">aspect_name</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubRetentionUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **entity_name** (`str`)
  * **aspect_name** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubRetention'`]]* *= 'dataHubRetention'*

#### *property* aspect_name *: str*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_name *: str*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubRetentionKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubRetentionKeyClass)) – 
* **Return type:**
  [`DataHubRetentionUrn`](#datahub.metadata.urns.DataHubRetentionUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubRetentionKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubRetentionKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubRoleUrn</span> {#datahub.metadata.urns.DataHubRoleUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubRoleUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubRoleUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubRoleUrn`](#datahub.metadata.urns.DataHubRoleUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubRole'`]]* *= 'dataHubRole'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubRoleKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubRoleKeyClass)) – 
* **Return type:**
  [`DataHubRoleUrn`](#datahub.metadata.urns.DataHubRoleUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubRoleKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubRoleKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubSecretUrn</span> {#datahub.metadata.urns.DataHubSecretUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubSecretUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubSecretUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubSecretUrn`](#datahub.metadata.urns.DataHubSecretUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubSecret'`]]* *= 'dataHubSecret'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubSecretKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubSecretKeyClass)) – 
* **Return type:**
  [`DataHubSecretUrn`](#datahub.metadata.urns.DataHubSecretUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubSecretKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubSecretKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubStepStateUrn</span> {#datahub.metadata.urns.DataHubStepStateUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubStepStateUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubStepStateUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubStepStateUrn`](#datahub.metadata.urns.DataHubStepStateUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubStepState'`]]* *= 'dataHubStepState'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubStepStateKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubStepStateKeyClass)) – 
* **Return type:**
  [`DataHubStepStateUrn`](#datahub.metadata.urns.DataHubStepStateUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubStepStateKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubStepStateKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubUpgradeUrn</span> {#datahub.metadata.urns.DataHubUpgradeUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubUpgradeUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubUpgradeUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubUpgradeUrn`](#datahub.metadata.urns.DataHubUpgradeUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubUpgrade'`]]* *= 'dataHubUpgrade'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubUpgradeKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubUpgradeKeyClass)) – 
* **Return type:**
  [`DataHubUpgradeUrn`](#datahub.metadata.urns.DataHubUpgradeUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubUpgradeKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubUpgradeKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataHubViewUrn</span> {#datahub.metadata.urns.DataHubViewUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataHubViewUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataHubViewUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataHubViewUrn`](#datahub.metadata.urns.DataHubViewUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataHubView'`]]* *= 'dataHubView'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataHubViewKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubViewKeyClass)) – 
* **Return type:**
  [`DataHubViewUrn`](#datahub.metadata.urns.DataHubViewUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataHubViewKeyClass`](models.mdx#datahub.metadata.schema_classes.DataHubViewKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataJobUrn</span> {#datahub.metadata.urns.DataJobUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataJobUrn</span>(<span class="arg-name">flow</span>, <span class="arg-name">job_id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataJobUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **flow** (`Union`[[`DataFlowUrn`](#datahub.metadata.urns.DataFlowUrn), `str`]) – 
  * **job_id** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataJob'`]]* *= 'dataJob'*

#### *classmethod* create_from_ids(data_flow_urn, job_id)

* **Parameters:**
  * **data_flow_urn** (`str`)
  * **job_id** (`str`)
* **Return type:**
  [`DataJobUrn`](#datahub.metadata.urns.DataJobUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *property* flow *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataJobKeyClass`](models.mdx#datahub.metadata.schema_classes.DataJobKeyClass)) – 
* **Return type:**
  [`DataJobUrn`](#datahub.metadata.urns.DataJobUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### get_data_flow_urn()

* **Return type:**
  [`DataFlowUrn`](#datahub.metadata.urns.DataFlowUrn)

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### get_job_id()

#### Deprecated
Deprecated since version 0.12.0.2: Use .job_id instead

* **Return type:**
  `str`

#### *property* job_id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* orchestrator *: str*

#### to_key_aspect()

* **Return type:**
  [`DataJobKeyClass`](models.mdx#datahub.metadata.schema_classes.DataJobKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataPlatformInstanceUrn</span> {#datahub.metadata.urns.DataPlatformInstanceUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataPlatformInstanceUrn</span>(<span class="arg-name">platform</span>, <span class="arg-name">instance</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataPlatformInstanceUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **platform** (`Union`[[`DataPlatformUrn`](#datahub.metadata.urns.DataPlatformUrn), `str`]) – 
  * **instance** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataPlatformInstance'`]]* *= 'dataPlatformInstance'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataPlatformInstanceKeyClass`](models.mdx#datahub.metadata.schema_classes.DataPlatformInstanceKeyClass)) – 
* **Return type:**
  [`DataPlatformInstanceUrn`](#datahub.metadata.urns.DataPlatformInstanceUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* instance *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* platform *: str*

#### to_key_aspect()

* **Return type:**
  [`DataPlatformInstanceKeyClass`](models.mdx#datahub.metadata.schema_classes.DataPlatformInstanceKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataPlatformUrn</span> {#datahub.metadata.urns.DataPlatformUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataPlatformUrn</span>(<span class="arg-name">platform_name</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataPlatformUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **platform_name** (`Union`[[`DataPlatformUrn`](#datahub.metadata.urns.DataPlatformUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataPlatform'`]]* *= 'dataPlatform'*

#### *classmethod* create_from_id(id)

#### Deprecated
Deprecated since version 0.12.0.2: Use the constructor instead

* **Parameters:**
  **id** (`str`)
* **Return type:**
  [`DataPlatformUrn`](#datahub.metadata.urns.DataPlatformUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataPlatformKeyClass`](models.mdx#datahub.metadata.schema_classes.DataPlatformKeyClass)) – 
* **Return type:**
  [`DataPlatformUrn`](#datahub.metadata.urns.DataPlatformUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* platform_name *: str*

#### to_key_aspect()

* **Return type:**
  [`DataPlatformKeyClass`](models.mdx#datahub.metadata.schema_classes.DataPlatformKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataProcessInstanceUrn</span> {#datahub.metadata.urns.DataProcessInstanceUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataProcessInstanceUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataProcessInstanceUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataProcessInstanceUrn`](#datahub.metadata.urns.DataProcessInstanceUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataProcessInstance'`]]* *= 'dataProcessInstance'*

#### *classmethod* create_from_id(id)

#### Deprecated
Deprecated since version 0.12.0.2: Use the constructor instead

* **Parameters:**
  **id** (`str`)
* **Return type:**
  [`DataProcessInstanceUrn`](#datahub.metadata.urns.DataProcessInstanceUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataProcessInstanceKeyClass`](models.mdx#datahub.metadata.schema_classes.DataProcessInstanceKeyClass)) – 
* **Return type:**
  [`DataProcessInstanceUrn`](#datahub.metadata.urns.DataProcessInstanceUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### get_dataprocessinstance_id()

#### Deprecated
Deprecated since version 0.12.0.2: Use .id instead

* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataProcessInstanceKeyClass`](models.mdx#datahub.metadata.schema_classes.DataProcessInstanceKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataProcessUrn</span> {#datahub.metadata.urns.DataProcessUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataProcessUrn</span>(<span class="arg-name">name</span>, <span class="arg-name">orchestrator</span>, <span class="arg-name">env</span> = <span class="arg-default">'PROD'</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataProcessUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **name** (`str`)
  * **orchestrator** (`str`)
  * **env** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataProcess'`]]* *= 'dataProcess'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *property* env *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataProcessKeyClass`](models.mdx#datahub.metadata.schema_classes.DataProcessKeyClass)) – 
* **Return type:**
  [`DataProcessUrn`](#datahub.metadata.urns.DataProcessUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### *property* orchestrator *: str*

#### to_key_aspect()

* **Return type:**
  [`DataProcessKeyClass`](models.mdx#datahub.metadata.schema_classes.DataProcessKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataProductUrn</span> {#datahub.metadata.urns.DataProductUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataProductUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataProductUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataProductUrn`](#datahub.metadata.urns.DataProductUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataProduct'`]]* *= 'dataProduct'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataProductKeyClass`](models.mdx#datahub.metadata.schema_classes.DataProductKeyClass)) – 
* **Return type:**
  [`DataProductUrn`](#datahub.metadata.urns.DataProductUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataProductKeyClass`](models.mdx#datahub.metadata.schema_classes.DataProductKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DataTypeUrn</span> {#datahub.metadata.urns.DataTypeUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DataTypeUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DataTypeUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DataTypeUrn`](#datahub.metadata.urns.DataTypeUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataType'`]]* *= 'dataType'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DataTypeKeyClass`](models.mdx#datahub.metadata.schema_classes.DataTypeKeyClass)) – 
* **Return type:**
  [`DataTypeUrn`](#datahub.metadata.urns.DataTypeUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DataTypeKeyClass`](models.mdx#datahub.metadata.schema_classes.DataTypeKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DatasetUrn</span> {#datahub.metadata.urns.DatasetUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DatasetUrn</span>(<span class="arg-name">platform</span>, <span class="arg-name">name</span>, <span class="arg-name">env</span> = <span class="arg-default">'PROD'</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DatasetUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **platform** (`Union`[[`DataPlatformUrn`](#datahub.metadata.urns.DataPlatformUrn), `str`]) – 
  * **name** (`str`)
  * **env** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'dataset'`]]* *= 'dataset'*

#### *classmethod* create_from_ids(platform_id, table_name, env, platform_instance=None)

* **Parameters:**
  * **platform_id** (`str`)
  * **table_name** (`str`)
  * **env** (`str`)
  * **platform_instance** (`Optional`[`str`])
* **Return type:**
  [`DatasetUrn`](#datahub.metadata.urns.DatasetUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *property* env *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DatasetKeyClass`](models.mdx#datahub.metadata.schema_classes.DatasetKeyClass)) – 
* **Return type:**
  [`DatasetUrn`](#datahub.metadata.urns.DatasetUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### get_data_platform_urn()

* **Return type:**
  [`DataPlatformUrn`](#datahub.metadata.urns.DataPlatformUrn)

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### get_dataset_name()

#### Deprecated
Deprecated since version 0.12.0.2: Use .name instead

* **Return type:**
  `str`

#### get_env()

#### Deprecated
Deprecated since version 0.12.0.2: Use .env instead

* **Return type:**
  `str`

#### *static* get_simple_field_path_from_v2_field_path()

A helper function to extract simple . path notation from the v2 field path

#### Deprecated
Deprecated since version 0.12.0.2: Use the function from the field_paths module instead

* **Parameters:**
  **field_path** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### *property* platform *: str*

#### to_key_aspect()

* **Return type:**
  [`DatasetKeyClass`](models.mdx#datahub.metadata.schema_classes.DatasetKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">DomainUrn</span> {#datahub.metadata.urns.DomainUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">DomainUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.DomainUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`DomainUrn`](#datahub.metadata.urns.DomainUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'domain'`]]* *= 'domain'*

#### *classmethod* create_from_id(id)

#### Deprecated
Deprecated since version 0.12.0.2: Use the constructor instead

* **Parameters:**
  **id** (`str`)
* **Return type:**
  [`DomainUrn`](#datahub.metadata.urns.DomainUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`DomainKeyClass`](models.mdx#datahub.metadata.schema_classes.DomainKeyClass)) – 
* **Return type:**
  [`DomainUrn`](#datahub.metadata.urns.DomainUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`DomainKeyClass`](models.mdx#datahub.metadata.schema_classes.DomainKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">EntityTypeUrn</span> {#datahub.metadata.urns.EntityTypeUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">EntityTypeUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.EntityTypeUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`EntityTypeUrn`](#datahub.metadata.urns.EntityTypeUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'entityType'`]]* *= 'entityType'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`EntityTypeKeyClass`](models.mdx#datahub.metadata.schema_classes.EntityTypeKeyClass)) – 
* **Return type:**
  [`EntityTypeUrn`](#datahub.metadata.urns.EntityTypeUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`EntityTypeKeyClass`](models.mdx#datahub.metadata.schema_classes.EntityTypeKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">ErModelRelationshipUrn</span> {#datahub.metadata.urns.ErModelRelationshipUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">ErModelRelationshipUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.ErModelRelationshipUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`ErModelRelationshipUrn`](#datahub.metadata.urns.ErModelRelationshipUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'erModelRelationship'`]]* *= 'erModelRelationship'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`ERModelRelationshipKeyClass`](models.mdx#datahub.metadata.schema_classes.ERModelRelationshipKeyClass)) – 
* **Return type:**
  [`ErModelRelationshipUrn`](#datahub.metadata.urns.ErModelRelationshipUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`ERModelRelationshipKeyClass`](models.mdx#datahub.metadata.schema_classes.ERModelRelationshipKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">FormUrn</span> {#datahub.metadata.urns.FormUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">FormUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.FormUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`FormUrn`](#datahub.metadata.urns.FormUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'form'`]]* *= 'form'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`FormKeyClass`](models.mdx#datahub.metadata.schema_classes.FormKeyClass)) – 
* **Return type:**
  [`FormUrn`](#datahub.metadata.urns.FormUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`FormKeyClass`](models.mdx#datahub.metadata.schema_classes.FormKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">GlobalSettingsUrn</span> {#datahub.metadata.urns.GlobalSettingsUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">GlobalSettingsUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.GlobalSettingsUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`GlobalSettingsUrn`](#datahub.metadata.urns.GlobalSettingsUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'globalSettings'`]]* *= 'globalSettings'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`GlobalSettingsKeyClass`](models.mdx#datahub.metadata.schema_classes.GlobalSettingsKeyClass)) – 
* **Return type:**
  [`GlobalSettingsUrn`](#datahub.metadata.urns.GlobalSettingsUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`GlobalSettingsKeyClass`](models.mdx#datahub.metadata.schema_classes.GlobalSettingsKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">GlossaryNodeUrn</span> {#datahub.metadata.urns.GlossaryNodeUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">GlossaryNodeUrn</span>(<span class="arg-name">name</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.GlossaryNodeUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **name** (`Union`[[`GlossaryNodeUrn`](#datahub.metadata.urns.GlossaryNodeUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'glossaryNode'`]]* *= 'glossaryNode'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`GlossaryNodeKeyClass`](models.mdx#datahub.metadata.schema_classes.GlossaryNodeKeyClass)) – 
* **Return type:**
  [`GlossaryNodeUrn`](#datahub.metadata.urns.GlossaryNodeUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### to_key_aspect()

* **Return type:**
  [`GlossaryNodeKeyClass`](models.mdx#datahub.metadata.schema_classes.GlossaryNodeKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">GlossaryTermUrn</span> {#datahub.metadata.urns.GlossaryTermUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">GlossaryTermUrn</span>(<span class="arg-name">name</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.GlossaryTermUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **name** (`Union`[[`GlossaryTermUrn`](#datahub.metadata.urns.GlossaryTermUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'glossaryTerm'`]]* *= 'glossaryTerm'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`GlossaryTermKeyClass`](models.mdx#datahub.metadata.schema_classes.GlossaryTermKeyClass)) – 
* **Return type:**
  [`GlossaryTermUrn`](#datahub.metadata.urns.GlossaryTermUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### to_key_aspect()

* **Return type:**
  [`GlossaryTermKeyClass`](models.mdx#datahub.metadata.schema_classes.GlossaryTermKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">IncidentUrn</span> {#datahub.metadata.urns.IncidentUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">IncidentUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.IncidentUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`IncidentUrn`](#datahub.metadata.urns.IncidentUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'incident'`]]* *= 'incident'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`IncidentKeyClass`](models.mdx#datahub.metadata.schema_classes.IncidentKeyClass)) – 
* **Return type:**
  [`IncidentUrn`](#datahub.metadata.urns.IncidentUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`IncidentKeyClass`](models.mdx#datahub.metadata.schema_classes.IncidentKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">InviteTokenUrn</span> {#datahub.metadata.urns.InviteTokenUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">InviteTokenUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.InviteTokenUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`InviteTokenUrn`](#datahub.metadata.urns.InviteTokenUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'inviteToken'`]]* *= 'inviteToken'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`InviteTokenKeyClass`](models.mdx#datahub.metadata.schema_classes.InviteTokenKeyClass)) – 
* **Return type:**
  [`InviteTokenUrn`](#datahub.metadata.urns.InviteTokenUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`InviteTokenKeyClass`](models.mdx#datahub.metadata.schema_classes.InviteTokenKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">MlFeatureTableUrn</span> {#datahub.metadata.urns.MlFeatureTableUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">MlFeatureTableUrn</span>(<span class="arg-name">platform</span>, <span class="arg-name">name</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.MlFeatureTableUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **platform** (`Union`[[`DataPlatformUrn`](#datahub.metadata.urns.DataPlatformUrn), `str`]) – 
  * **name** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'mlFeatureTable'`]]* *= 'mlFeatureTable'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`MLFeatureTableKeyClass`](models.mdx#datahub.metadata.schema_classes.MLFeatureTableKeyClass)) – 
* **Return type:**
  [`MlFeatureTableUrn`](#datahub.metadata.urns.MlFeatureTableUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### *property* platform *: str*

#### to_key_aspect()

* **Return type:**
  [`MLFeatureTableKeyClass`](models.mdx#datahub.metadata.schema_classes.MLFeatureTableKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">MlFeatureUrn</span> {#datahub.metadata.urns.MlFeatureUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">MlFeatureUrn</span>(<span class="arg-name">feature_namespace</span>, <span class="arg-name">name</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.MlFeatureUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **feature_namespace** (`str`)
  * **name** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'mlFeature'`]]* *= 'mlFeature'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *property* feature_namespace *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`MLFeatureKeyClass`](models.mdx#datahub.metadata.schema_classes.MLFeatureKeyClass)) – 
* **Return type:**
  [`MlFeatureUrn`](#datahub.metadata.urns.MlFeatureUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### to_key_aspect()

* **Return type:**
  [`MLFeatureKeyClass`](models.mdx#datahub.metadata.schema_classes.MLFeatureKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">MlModelDeploymentUrn</span> {#datahub.metadata.urns.MlModelDeploymentUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">MlModelDeploymentUrn</span>(<span class="arg-name">platform</span>, <span class="arg-name">name</span>, <span class="arg-name">env</span> = <span class="arg-default">'PROD'</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.MlModelDeploymentUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **platform** (`Union`[[`DataPlatformUrn`](#datahub.metadata.urns.DataPlatformUrn), `str`]) – 
  * **name** (`str`)
  * **env** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'mlModelDeployment'`]]* *= 'mlModelDeployment'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *property* env *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`MLModelDeploymentKeyClass`](models.mdx#datahub.metadata.schema_classes.MLModelDeploymentKeyClass)) – 
* **Return type:**
  [`MlModelDeploymentUrn`](#datahub.metadata.urns.MlModelDeploymentUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### *property* platform *: str*

#### to_key_aspect()

* **Return type:**
  [`MLModelDeploymentKeyClass`](models.mdx#datahub.metadata.schema_classes.MLModelDeploymentKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">MlModelGroupUrn</span> {#datahub.metadata.urns.MlModelGroupUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">MlModelGroupUrn</span>(<span class="arg-name">platform</span>, <span class="arg-name">name</span>, <span class="arg-name">env</span> = <span class="arg-default">'PROD'</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.MlModelGroupUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **platform** (`Union`[[`DataPlatformUrn`](#datahub.metadata.urns.DataPlatformUrn), `str`]) – 
  * **name** (`str`)
  * **env** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'mlModelGroup'`]]* *= 'mlModelGroup'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *property* env *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`MLModelGroupKeyClass`](models.mdx#datahub.metadata.schema_classes.MLModelGroupKeyClass)) – 
* **Return type:**
  [`MlModelGroupUrn`](#datahub.metadata.urns.MlModelGroupUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### *property* platform *: str*

#### to_key_aspect()

* **Return type:**
  [`MLModelGroupKeyClass`](models.mdx#datahub.metadata.schema_classes.MLModelGroupKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">MlModelUrn</span> {#datahub.metadata.urns.MlModelUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">MlModelUrn</span>(<span class="arg-name">platform</span>, <span class="arg-name">name</span>, <span class="arg-name">env</span> = <span class="arg-default">'PROD'</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.MlModelUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **platform** (`Union`[[`DataPlatformUrn`](#datahub.metadata.urns.DataPlatformUrn), `str`]) – 
  * **name** (`str`)
  * **env** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'mlModel'`]]* *= 'mlModel'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *property* env *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`MLModelKeyClass`](models.mdx#datahub.metadata.schema_classes.MLModelKeyClass)) – 
* **Return type:**
  [`MlModelUrn`](#datahub.metadata.urns.MlModelUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### *property* platform *: str*

#### to_key_aspect()

* **Return type:**
  [`MLModelKeyClass`](models.mdx#datahub.metadata.schema_classes.MLModelKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">MlPrimaryKeyUrn</span> {#datahub.metadata.urns.MlPrimaryKeyUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">MlPrimaryKeyUrn</span>(<span class="arg-name">feature_namespace</span>, <span class="arg-name">name</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.MlPrimaryKeyUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **feature_namespace** (`str`)
  * **name** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'mlPrimaryKey'`]]* *= 'mlPrimaryKey'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *property* feature_namespace *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`MLPrimaryKeyKeyClass`](models.mdx#datahub.metadata.schema_classes.MLPrimaryKeyKeyClass)) – 
* **Return type:**
  [`MlPrimaryKeyUrn`](#datahub.metadata.urns.MlPrimaryKeyUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### to_key_aspect()

* **Return type:**
  [`MLPrimaryKeyKeyClass`](models.mdx#datahub.metadata.schema_classes.MLPrimaryKeyKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">NotebookUrn</span> {#datahub.metadata.urns.NotebookUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">NotebookUrn</span>(<span class="arg-name">notebook_tool</span>, <span class="arg-name">notebook_id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.NotebookUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **notebook_tool** (`str`)
  * **notebook_id** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'notebook'`]]* *= 'notebook'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`NotebookKeyClass`](models.mdx#datahub.metadata.schema_classes.NotebookKeyClass)) – 
* **Return type:**
  [`NotebookUrn`](#datahub.metadata.urns.NotebookUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### get_notebook_id()

#### Deprecated
Deprecated since version 0.12.0.2: Use .notebook_id instead

* **Return type:**
  `str`

#### get_platform_id()

#### Deprecated
Deprecated since version 0.12.0.2: Use .notebook_tool instead

* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* notebook_id *: str*

#### *property* notebook_tool *: str*

#### to_key_aspect()

* **Return type:**
  [`NotebookKeyClass`](models.mdx#datahub.metadata.schema_classes.NotebookKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">OwnershipTypeUrn</span> {#datahub.metadata.urns.OwnershipTypeUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">OwnershipTypeUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.OwnershipTypeUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`OwnershipTypeUrn`](#datahub.metadata.urns.OwnershipTypeUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'ownershipType'`]]* *= 'ownershipType'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`OwnershipTypeKeyClass`](models.mdx#datahub.metadata.schema_classes.OwnershipTypeKeyClass)) – 
* **Return type:**
  [`OwnershipTypeUrn`](#datahub.metadata.urns.OwnershipTypeUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`OwnershipTypeKeyClass`](models.mdx#datahub.metadata.schema_classes.OwnershipTypeKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">PlatformResourceUrn</span> {#datahub.metadata.urns.PlatformResourceUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">PlatformResourceUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.PlatformResourceUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`PlatformResourceUrn`](#datahub.metadata.urns.PlatformResourceUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'platformResource'`]]* *= 'platformResource'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`PlatformResourceKeyClass`](models.mdx#datahub.metadata.schema_classes.PlatformResourceKeyClass)) – 
* **Return type:**
  [`PlatformResourceUrn`](#datahub.metadata.urns.PlatformResourceUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`PlatformResourceKeyClass`](models.mdx#datahub.metadata.schema_classes.PlatformResourceKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">PostUrn</span> {#datahub.metadata.urns.PostUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">PostUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.PostUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`PostUrn`](#datahub.metadata.urns.PostUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'post'`]]* *= 'post'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`PostKeyClass`](models.mdx#datahub.metadata.schema_classes.PostKeyClass)) – 
* **Return type:**
  [`PostUrn`](#datahub.metadata.urns.PostUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`PostKeyClass`](models.mdx#datahub.metadata.schema_classes.PostKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">QueryUrn</span> {#datahub.metadata.urns.QueryUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">QueryUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.QueryUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`QueryUrn`](#datahub.metadata.urns.QueryUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'query'`]]* *= 'query'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`QueryKeyClass`](models.mdx#datahub.metadata.schema_classes.QueryKeyClass)) – 
* **Return type:**
  [`QueryUrn`](#datahub.metadata.urns.QueryUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`QueryKeyClass`](models.mdx#datahub.metadata.schema_classes.QueryKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">RoleUrn</span> {#datahub.metadata.urns.RoleUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">RoleUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.RoleUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`RoleUrn`](#datahub.metadata.urns.RoleUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'role'`]]* *= 'role'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`RoleKeyClass`](models.mdx#datahub.metadata.schema_classes.RoleKeyClass)) – 
* **Return type:**
  [`RoleUrn`](#datahub.metadata.urns.RoleUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`RoleKeyClass`](models.mdx#datahub.metadata.schema_classes.RoleKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">SchemaFieldUrn</span> {#datahub.metadata.urns.SchemaFieldUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">SchemaFieldUrn</span>(<span class="arg-name">parent</span>, <span class="arg-name">field_path</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.SchemaFieldUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **parent** (`Union`[[`Urn`](#datahub.metadata.urns.Urn), `str`]) – 
  * **field_path** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'schemaField'`]]* *= 'schemaField'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *property* field_path *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`SchemaFieldKeyClass`](models.mdx#datahub.metadata.schema_classes.SchemaFieldKeyClass)) – 
* **Return type:**
  [`SchemaFieldUrn`](#datahub.metadata.urns.SchemaFieldUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* parent *: str*

#### to_key_aspect()

* **Return type:**
  [`SchemaFieldKeyClass`](models.mdx#datahub.metadata.schema_classes.SchemaFieldKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">StructuredPropertyUrn</span> {#datahub.metadata.urns.StructuredPropertyUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">StructuredPropertyUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.StructuredPropertyUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`StructuredPropertyUrn`](#datahub.metadata.urns.StructuredPropertyUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'structuredProperty'`]]* *= 'structuredProperty'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`StructuredPropertyKeyClass`](models.mdx#datahub.metadata.schema_classes.StructuredPropertyKeyClass)) – 
* **Return type:**
  [`StructuredPropertyUrn`](#datahub.metadata.urns.StructuredPropertyUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`StructuredPropertyKeyClass`](models.mdx#datahub.metadata.schema_classes.StructuredPropertyKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">TagUrn</span> {#datahub.metadata.urns.TagUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">TagUrn</span>(<span class="arg-name">name</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.TagUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **name** (`Union`[[`TagUrn`](#datahub.metadata.urns.TagUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'tag'`]]* *= 'tag'*

#### *classmethod* create_from_id(id)

#### Deprecated
Deprecated since version 0.12.0.2: Use the constructor instead

* **Parameters:**
  **id** (`str`)
* **Return type:**
  [`TagUrn`](#datahub.metadata.urns.TagUrn)

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`TagKeyClass`](models.mdx#datahub.metadata.schema_classes.TagKeyClass)) – 
* **Return type:**
  [`TagUrn`](#datahub.metadata.urns.TagUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### to_key_aspect()

* **Return type:**
  [`TagKeyClass`](models.mdx#datahub.metadata.schema_classes.TagKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">TelemetryUrn</span> {#datahub.metadata.urns.TelemetryUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">TelemetryUrn</span>(<span class="arg-name">name</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.TelemetryUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **name** (`Union`[[`TelemetryUrn`](#datahub.metadata.urns.TelemetryUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'telemetry'`]]* *= 'telemetry'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`TelemetryKeyClass`](models.mdx#datahub.metadata.schema_classes.TelemetryKeyClass)) – 
* **Return type:**
  [`TelemetryUrn`](#datahub.metadata.urns.TelemetryUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### *property* name *: str*

#### to_key_aspect()

* **Return type:**
  [`TelemetryKeyClass`](models.mdx#datahub.metadata.schema_classes.TelemetryKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">TestUrn</span> {#datahub.metadata.urns.TestUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">TestUrn</span>(<span class="arg-name">id</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.TestUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`Union`[[`TestUrn`](#datahub.metadata.urns.TestUrn), `str`]) – 
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'test'`]]* *= 'test'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`TestKeyClass`](models.mdx#datahub.metadata.schema_classes.TestKeyClass)) – 
* **Return type:**
  [`TestUrn`](#datahub.metadata.urns.TestUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`TestKeyClass`](models.mdx#datahub.metadata.schema_classes.TestKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">Urn</span> {#datahub.metadata.urns.Urn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">Urn</span>(<span class="arg-name">entity_type</span>, <span class="arg-name">entity_id</span>)<a href="#datahub.metadata.urns.Urn" class="hash-link"></a></div>


Bases: `object`

URNs are globally unique identifiers used to refer to entities.

It will be in format of [urn:li](urn:li):&lt;type&gt;:&lt;id&gt; or [urn:li](urn:li):&lt;type&gt;:(&lt;id1&gt;,&lt;id2&gt;,…)

A note on encoding: certain characters, particularly commas and parentheses, are
not allowed in string portions of the URN. However, these are allowed when the urn
has another urn embedded within it. The main URN class ignores this possibility,
and assumes that the user provides a valid URN string. However, the specific URN
classes, such as DatasetUrn, will automatically encode these characters using
url-encoding when the URN is created and \_allow_coercion is enabled (the default).
However, all from_string methods will try to preserve the string as-is, and will
raise an error if the string is invalid.

* **Parameters:**
  * **entity_type** (`str`)
  * **entity_id** (`List`[`str`])
#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`



</div>




### <span className="visually-hidden">VersionSetUrn</span> {#datahub.metadata.urns.VersionSetUrn}


<div className="h3-block">

<div className="section-heading"><span class="class-text">class</span> <span class="class-owner">datahub.metadata.urns.</span><span class="class-name">VersionSetUrn</span>(<span class="arg-name">id</span>, <span class="arg-name">entity_type</span>, *, <span class="arg-name">_allow_coercion</span> = <span class="arg-default">True</span>)<a href="#datahub.metadata.urns.VersionSetUrn" class="hash-link"></a></div>


Bases: `_SpecificUrn`

* **Parameters:**
  * **id** (`str`)
  * **entity_type** (`str`)
  * **\_allow_coercion** (`bool`) – 

#### ENTITY_TYPE *: `ClassVar`[`Literal`[`'versionSet'`]]* *= 'versionSet'*

#### *classmethod* create_from_string(urn_str)

* **Parameters:**
  **urn_str** (`str`)
* **Return type:**
  `Self`

#### *property* entity_ids *: List[str]*

#### *property* entity_type *: str*

#### *classmethod* from_key_aspect(key_aspect)

* **Parameters:**
  **key_aspect** ([`VersionSetKeyClass`](models.mdx#datahub.metadata.schema_classes.VersionSetKeyClass)) – 
* **Return type:**
  [`VersionSetUrn`](#datahub.metadata.urns.VersionSetUrn)

#### *classmethod* from_string(urn_str, /)

Create an Urn from its string representation.

When called against the base Urn class, this method will return a more specific Urn type where possible.

```pycon
>>> from datahub.metadata.urns import DatasetUrn, Urn
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> urn = Urn.from_string(urn_str)
>>> assert isinstance(urn, DatasetUrn)
```

When called against a specific Urn type (e.g. DatasetUrn.from_string), this method can
also be used for type narrowing.

```pycon
>>> urn_str = 'urn:li:dataset:(urn:li:dataPlatform:snowflake,my_db.my_schema.my_table,PROD)'
>>> assert DatasetUrn.from_string(urn_str)
```

* **Parameters:**
  **urn_str** (`Union`[`str`, [`Urn`](#datahub.metadata.urns.Urn)]) – The string representation of the urn. Also accepts an existing Urn instance.
* **Return type:**
  `Self`
* **Returns:**
  Urn of the given string representation.
* **Raises:**
  **InvalidUrnError** – If the string representation is in invalid format.

#### *static* get_data_type_from_urn(urn)

* **Parameters:**
  **urn** (`str`)
* **Return type:**
  `str`

#### *property* id *: str*

#### *static* make_data_type_urn(type)

* **Parameters:**
  **type** (`str`)
* **Return type:**
  `str`

#### *static* make_entity_type_urn(entity_type)

* **Parameters:**
  **entity_type** (`str`)
* **Return type:**
  `str`

#### *static* make_form_urn(form)

* **Parameters:**
  **form** (`str`)
* **Return type:**
  `str`

#### *static* make_structured_property_urn(structured_property)

* **Parameters:**
  **structured_property** (`str`)
* **Return type:**
  `str`

#### to_key_aspect()

* **Return type:**
  [`VersionSetKeyClass`](models.mdx#datahub.metadata.schema_classes.VersionSetKeyClass)

#### urn()

Get the string representation of the urn.

* **Return type:**
  `str`

#### urn_url_encoded()

* **Return type:**
  `str`


</div>
</div>
