tenacity==9.1.2
requests==2.32.4

# Runs the sphinx -> docusaurus converter tests (see sphinx/tests).
# Range rather than a pin: the tests are plain stdlib and pin nothing themselves.
pytest>=6.2.2,<10.0.0
