name: harshal_hello_world_action
source:
  type: kafka
  config:
    connection:
      bootstrap: localhost:9092
      schema_registry_url: http://localhost:8080/schema-registry/api/
datahub:
  server: "http://localhost:8080"
# filter:
#   event_type: EntityChangeEvent_v1

# action:
#   type: datahub_integrations.propagation.doc.doc_propagation_action.DocPropagationAction
#   config: {}
action:
  type: datahub_integrations.actions.recording_action.RecordingAction
  config:
    filename: ./test_recording.json
# pipeline_name: "urn:li:action:doc_propagation_action"
