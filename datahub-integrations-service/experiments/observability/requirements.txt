# Requirements for experimental observability tools (dashboard + chat UI)
# These are isolated from production dependencies

# Core dependencies
streamlit>=1.40.0
plotly>=5.24.0
duckdb>=1.0.0
pandas>=2.2.0
requests>=2.32.0
boto3>=1.35.0
loguru>=0.7.0

# NOTE: datahub-integrations will be installed separately in setup script
