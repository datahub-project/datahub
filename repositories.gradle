repositories {    
    mavenLocal()        
    maven {
        url "https://artifacts.sys.dom:443/artifactory/maven"
    }
}

try {
    subprojects {
        project.repositories.addAll(rootProject.repositories)
    }
} catch (Throwable t) {
}

