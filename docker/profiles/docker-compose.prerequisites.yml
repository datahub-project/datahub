# Common environment
x-search-datastore-search: &search-datastore-environment
  ELASTICSEARCH_HOST: search
  ELASTICSEARCH_PORT: 9200
  ELASTICSEARCH_PROTOCOL: http
  ELASTICSEARCH_USE_SSL: ${ELASTICSEARCH_USE_SSL:-false}

# Primary Storage Profiles
x-mysql-profiles-quickstart: &mysql-profiles-quickstart
  - quickstart
  - quickstart-backend
  - quickstart-frontend
  - quickstart-storage
  - quickstart-consumers

# Which profiles to run mysql setup
x-mysql-setup-profiles-dev: &mysql-setup-profiles-dev
  - debug-min
  - debug-frontend
  - debug-backend
  - debug-consumers
  - debug-neo4j
  - debug-elasticsearch
  - debug-backend-aws
  - debug-aws

x-mysql-profiles: &mysql-profiles
  - quickstart
  - quickstart-backend
  - quickstart-frontend
  - quickstart-storage
  - quickstart-consumers
  - quickstart-consumers-cdc
  - debug
  - debug-min
  - debug-datahub-actions
  - debug-frontend
  - debug-backend
  - debug-consumers
  - debug-consumers-cdc
  - debug-neo4j
  - debug-elasticsearch
  - debug-backend-aws
  - debug-aws
x-mysql-profiles-cdc: &mysql-profiles-cdc
  - quickstart-consumers-cdc
  - debug-consumers-cdc
x-postgres-profiles-quickstart: &postgres-profiles-quickstart
  - quickstart-postgres
x-postgres-profiles-dev: &postgres-profiles-dev
  - debug-postgres
x-postgres-profiles: &postgres-profiles
  - quickstart-postgres
  - quickstart-postgres-cdc
  - debug-postgres
  - debug-postgres-cdc

x-cassandra-profiles: &cassandra-profiles
  - quickstart-cassandra
  - debug-cassandra

# Graph Storage Profiles
x-neo4j-profiles: &neo4j-profiles
  - quickstart-cassandra
  - debug-neo4j

# Search Storage Profiles
x-elasticsearch-profiles: &elasticsearch-profiles
  - debug-elasticsearch

x-opensearch-profiles-quickstart: &opensearch-profiles-quickstart
  - quickstart
  - quickstart-datahub-actions
  - quickstart-backend
  - quickstart-frontend
  - quickstart-storage
  - quickstart-cassandra
  - quickstart-postgres
  - quickstart-postgres-cdc
  - quickstart-consumers
  - quickstart-consumers-cdc
x-opensearch-setup-profiles-dev: &opensearch-setup-profiles-dev
  - debug-min
  - debug-datahub-actions
  - debug-frontend
  - debug-backend
  - debug-postgres
  - debug-postgres-cdc
  - debug-cassandra
  - debug-consumers
  - debug-consumers-cdc
  - debug-neo4j
  - debug-backend-aws
  - debug-aws
x-opensearch-profiles: &opensearch-profiles
  - quickstart
  - quickstart-backend
  - quickstart-actions
  - quickstart-frontend
  - quickstart-storage
  - quickstart-cassandra
  - quickstart-postgres
  - quickstart-postgres-cdc
  - quickstart-consumers
  - quickstart-consumers-cdc
  - debug
  - debug-min
  - debug-datahub-actions
  - debug-frontend
  - debug-backend
  - debug-postgres
  - debug-postgres-cdc
  - debug-cassandra
  - debug-consumers
  - debug-consumers-cdc
  - debug-neo4j
  - debug-backend-aws
  - debug-aws

# AWS Infra profiles
x-profiles-aws: &aws-profiles
  - debug-backend-aws
  - debug-aws

# Debug vs Quickstart Profiles
x-profiles-quickstart: &profiles-quickstart
  - quickstart
  - quickstart-backend
  - quickstart-actions
  - quickstart-frontend
  - quickstart-storage
  - quickstart-cassandra
  - quickstart-postgres
  - quickstart-postgres-cdc
  - quickstart-consumers
  - quickstart-consumers-cdc
x-profiles-dev: &profiles-dev
  - debug
  - debug-min
  - debug-datahub-actions
  - debug-frontend
  - debug-backend
  - debug-postgres
  - debug-postgres-cdc
  - debug-cassandra
  - debug-consumers
  - debug-consumers-cdc
  - debug-neo4j
  - debug-elasticsearch
  - debug-backend-aws
  - debug-aws
x-mysql-setup-common: &mysql-setup-common
  hostname: mysql-setup
  image: ${DATAHUB_MYSQL_SETUP_IMAGE:-${DATAHUB_REPO:-acryldata}/datahub-mysql-setup}:${DATAHUB_VERSION:-head}
  env_file:
    - mysql-setup/env/docker.env
  depends_on:
    mysql:
      condition: service_healthy
  labels:
    datahub_setup_job: true
x-postgres-setup-common: &postgres-setup-common
  hostname: postgres-setup
  image: ${DATAHUB_POSTGRES_SETUP_IMAGE:-${DATAHUB_REPO:-acryldata}/datahub-postgres-setup}:${DATAHUB_VERSION:-head}
  env_file: postgres-setup/env/docker.env
  depends_on:
    postgres:
      condition: service_healthy
  labels:
    datahub_setup_job: true

services:
  mysql:
    profiles: *mysql-profiles
    hostname: mysql
    image: mysql:${DATAHUB_MYSQL_VERSION:-8.2}
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_bin --default-authentication-plugin=caching_sha2_password
    ports:
      - ${DATAHUB_MAPPED_MYSQL_PORT:-3306}:3306
    env_file: mysql/env/docker.env
    restart: on-failure
    healthcheck:
      test: mysqladmin ping -h mysql -u $$MYSQL_USER --password=$$MYSQL_PASSWORD
      start_period: 20s
      interval: 2s
      timeout: 10s
      retries: 5
    volumes:
      - mysqldata:/var/lib/mysql
  mysql-setup:
    <<: *mysql-setup-common
    profiles: *mysql-profiles-quickstart
  mysql-setup-cdc:
    <<: *mysql-setup-common
    environment: &mysql-setup-env-cdc
      CDC_MCL_PROCESSING_ENABLED: ${CDC_MCL_PROCESSING_ENABLED:-true}
    profiles:
      - quickstart-consumers-cdc
  mysql-setup-dev:
    <<: *mysql-setup-common
    profiles: *mysql-setup-profiles-dev
    image: ${DATAHUB_MYSQL_SETUP_IMAGE:-${DATAHUB_REPO:-acryldata}/datahub-mysql-setup}:${DATAHUB_VERSION:-debug}
  mysql-setup-dev-cdc:
    <<: *mysql-setup-common
    profiles:
      - debug-consumers-cdc
    image: ${DATAHUB_MYSQL_SETUP_IMAGE:-${DATAHUB_REPO:-acryldata}/datahub-mysql-setup}:${DATAHUB_VERSION:-debug}
    environment:
      <<: *mysql-setup-env-cdc
  postgres:
    profiles: *postgres-profiles
    hostname: postgres
    image: postgres:${DATAHUB_POSTGRES_VERSION:-15.5}
    command: postgres -c wal_level=logical -c max_replication_slots=4 -c max_wal_senders=4 -c shared_preload_libraries=pgoutput
    env_file: postgres/env/docker.env
    ports:
      - '5432:5432'
    restart: on-failure
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready" ]
      start_period: 20s
      interval: 2s
      timeout: 10s
      retries: 5
    volumes:
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
      - postgresdata:/var/lib/postgresql/data
  postgres-setup:
    <<: *postgres-setup-common
    profiles: *postgres-profiles-quickstart
  postgres-setup-cdc:
    <<: *postgres-setup-common
    environment: &postgres-setup-env-cdc
      CDC_MCL_PROCESSING_ENABLED: ${CDC_MCL_PROCESSING_ENABLED:-true}
    profiles:
      - quickstart-postgres-cdc
  postgres-setup-dev:
    <<: *postgres-setup-common
    profiles: *postgres-profiles-dev
    image: ${DATAHUB_POSTGRES_SETUP_IMAGE:-${DATAHUB_REPO:-acryldata}/datahub-postgres-setup}:${DATAHUB_VERSION:-debug}
  postgres-setup-dev-cdc:
    <<: *postgres-setup-common
    profiles:
      - debug-postgres-cdc
    image: ${DATAHUB_POSTGRES_SETUP_IMAGE:-${DATAHUB_REPO:-acryldata}/datahub-postgres-setup}:${DATAHUB_VERSION:-debug}
    environment:
      <<: *postgres-setup-env-cdc
  cassandra:
    profiles: *cassandra-profiles
    hostname: cassandra
    image: cassandra:4.1
    ports:
      - 9042:9042
    healthcheck:
      test: cqlsh -u cassandra -p cassandra -e 'describe keyspaces'
      interval: 15s
      timeout: 10s
      retries: 10
    volumes:
      - cassandradata:/var/lib/cassandra
  cassandra-setup:
    profiles: *cassandra-profiles
    hostname: cassandra-setup
    image: cassandra:4.1
    command: /bin/bash -c "cqlsh cassandra -f /init.cql"
    depends_on:
      cassandra:
        condition: service_healthy
    volumes:
      - ./cassandra/init.cql:/init.cql
    labels:
      datahub_setup_job: true
  neo4j:
    profiles: *neo4j-profiles
    hostname: neo4j
    image: neo4j:4.4.28-community
    ports:
      - ${DATAHUB_MAPPED_NEO4J_HTTP_PORT:-7474}:7474
      - ${DATAHUB_MAPPED_NEO4J_BOLT_PORT:-7687}:7687
    env_file: neo4j/env/docker.env
    healthcheck:
      test: wget http://neo4j:$${DATAHUB_NEO4J_HTTP_PORT:-7474}
      start_period: 5s
      interval: 1s
      retries: 5
      timeout: 5s
    volumes:
      - neo4jdata:/data
  kafka-broker:
    hostname: broker
    image: ${DATAHUB_CONFLUENT_KAFKA_IMAGE:-confluentinc/cp-kafka}:${DATAHUB_CONFLUENT_VERSION:-8.0.0}
    command:
      - /bin/bash
      - -c
      - |
        # Generate KRaft clusterID
        file_path="/var/lib/kafka/data/clusterID"
  
        if [ ! -f "$$file_path" ]; then
           /bin/kafka-storage random-uuid > $$file_path
           echo "Cluster id has been created..."
           # KRaft required step: Format the storage directory with a new cluster ID
           kafka-storage format --ignore-formatted -t $$(cat "$$file_path") -c /etc/kafka/kafka.properties
        fi
  
        export CLUSTER_ID=$$(cat "$$file_path")
        echo "CLUSTER_ID=$$CLUSTER_ID"
  
        /etc/confluent/docker/run
    ports:
      - ${DATAHUB_MAPPED_KAFKA_BROKER_PORT:-9092}:9092
    env_file: kafka-broker/env/docker.env
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_ADVERTISED_LISTENERS: BROKER://broker:29092,EXTERNAL://localhost:9092
      KAFKA_LISTENERS: BROKER://broker:29092,EXTERNAL://broker:9092,CONTROLLER://broker:39092
      KAFKA_INTER_BROKER_LISTENER_NAME: BROKER
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,BROKER:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_PROCESS_ROLES: controller, broker
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@broker:39092
      # https://github.com/confluentinc/cp-all-in-one/issues/120
      KAFKA_LOG4J_LOGGERS: 'org.apache.kafka.image.loader.MetadataLoader=WARN'
      KAFKA_ZOOKEEPER_CONNECT: null
    healthcheck:
      test: nc -z broker $${DATAHUB_KAFKA_BROKER_PORT:-9092}
      start_period: 60s
      interval: 1s
      retries: 5
      timeout: 5s
    volumes:
      - broker:/var/lib/kafka/data/
  kafka-connect:
    hostname: kafka-connect
    container_name: kafka_connect
    image: debezium/connect:2.7.3.Final
    #image: quay.io/debezium/connect:3.2
    profiles:
      - debug-consumers-cdc
      - quickstart-consumers-cdc
      - debug-postgres-cdc
      - quickstart-postgres-cdc
    ports:
      - '8083:8083'
    depends_on:
      kafka-broker:
        condition: service_healthy
    environment:
      - BOOTSTRAP_SERVERS=broker:29092
      - GROUP_ID=datahub_connect
      - CONFIG_STORAGE_TOPIC=${CDC_CONFIG_STORAGE_TOPIC:-datahub_cdc_configs}
      - OFFSET_STORAGE_TOPIC=${CDC_CONFIG_OFFSETS_TOPIC:-datahub_cdc_offsets}
      - STATUS_STORAGE_TOPIC=${CDC_CONFIG_STATUS_TOPIC:-datahub_cdc_statuses}
      - CONFIG_STORAGE_REPLICATION_FACTOR=1
      - OFFSET_STORAGE_REPLICATION_FACTOR=1
      - STATUS_STORAGE_REPLICATION_FACTOR=1
    healthcheck:
      test: curl -f http://localhost:8083/connectors
      start_period: 30s
      interval: 10s
      timeout: 5s
      retries: 5
  elasticsearch:
    profiles: *elasticsearch-profiles
    hostname: search
    image: ${DATAHUB_SEARCH_IMAGE:-elasticsearch}:${DATAHUB_SEARCH_TAG:-7.10.1}
    ports:
      - ${DATAHUB_MAPPED_ELASTIC_PORT:-9200}:9200
    env_file: elasticsearch/env/docker.env
    environment:
      - discovery.type=single-node
      - ${XPACK_SECURITY_ENABLED:-xpack.security.enabled=false}
    deploy:
      resources:
        limits:
          memory: 1G
    healthcheck:
      test: curl -sS --fail http://search:$${DATAHUB_ELASTIC_PORT:-9200}/_cluster/health?wait_for_status=yellow&timeout=0s
      start_period: 30s
      interval: 1s
      retries: 3
      timeout: 5s
    volumes:
      - esdata:/usr/share/elasticsearch/data
  elasticsearch-setup-dev: &elasticsearch-setup-dev
    image: ${DATAHUB_ELASTIC_SETUP_IMAGE:-${DATAHUB_REPO:-acryldata}/datahub-elasticsearch-setup}:${DATAHUB_VERSION:-debug}
    profiles: *elasticsearch-profiles
    hostname: elasticsearch-setup
    env_file: elasticsearch-setup/env/docker.env
    environment:
      <<: *search-datastore-environment
      USE_AWS_ELASTICSEARCH: ${USE_AWS_ELASTICSEARCH:-false}
    depends_on:
      elasticsearch:
        condition: service_healthy
    labels:
      datahub_setup_job: true
  opensearch:
    profiles: *opensearch-profiles
    hostname: search
    image: ${DATAHUB_SEARCH_IMAGE:-opensearchproject/opensearch}:${DATAHUB_SEARCH_TAG:-2.19.3}
    ports:
      - ${DATAHUB_MAPPED_ELASTIC_PORT:-9200}:9200
    env_file: elasticsearch/env/docker.env
    environment:
      - discovery.type=single-node
      - DISABLE_SECURITY_PLUGIN=true
    healthcheck:
      test: curl -sS --fail http://search:$${DATAHUB_ELASTIC_PORT:-9200}/_cluster/health?wait_for_status=yellow&timeout=0s
      start_period: 60s
      interval: 5s
      retries: 10
      timeout: 15s
    volumes:
      - osdata:/usr/share/opensearch/data
  opensearch-setup: &opensearch-setup
    <<: *elasticsearch-setup-dev
    profiles: *opensearch-profiles-quickstart
    hostname: opensearch-setup
    image: ${DATAHUB_ELASTIC_SETUP_IMAGE:-${DATAHUB_REPO:-acryldata}/datahub-elasticsearch-setup}:${DATAHUB_VERSION:-head}
    environment:
      <<: *search-datastore-environment
      USE_AWS_ELASTICSEARCH: ${USE_AWS_ELASTICSEARCH:-true}
    depends_on:
      opensearch:
        condition: service_healthy
    command: /bin/sh -c "/create-indices.sh"
    labels:
      datahub_setup_job: true
  opensearch-setup-dev:
    <<: *opensearch-setup
    profiles: *opensearch-setup-profiles-dev
    hostname: opensearch-setup-dev
    image: ${DATAHUB_ELASTIC_SETUP_IMAGE:-${DATAHUB_REPO:-acryldata}/datahub-elasticsearch-setup}:${DATAHUB_VERSION:-debug}
    environment:
      <<: *search-datastore-environment
      USE_AWS_ELASTICSEARCH: ${USE_AWS_ELASTICSEARCH:-true}
    depends_on:
      opensearch:
        condition: service_healthy
    command: /bin/sh -c "sleep 10 && /create-indices.sh"
  aws-localstack:
    profiles: *aws-profiles
    hostname: localstack
    image: ${LOCALSTACK_IMAGE:-localstack/localstack:4}
    ports:
      - ${DATAHUB_MAPPED_LOCALSTACK_PORT:-4566}:4566                     # LocalStack's main service port
    environment:
      - AWS_DEFAULT_REGION=${LOCALSTACK_REGION:-us-east-1}
      - SERVICES=${LOCALSTACK_SERVICES:-s3,sqs,iam,sts}
    volumes:
      - localstackdata:/var/lib/localstack
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4566/_localstack/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

networks:
  default:
    name: datahub_network

volumes:
  neo4jdata:
  esdata:
  osdata:
  broker:
  mysqldata:
  cassandradata:
  postgresdata:
  localstackdata:
