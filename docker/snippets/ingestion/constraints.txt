cmdstanpy>=1.3.0,<2.0.0
# GHSA-58pv-8j8x-9vj2: Path traversal vulnerability in jaraco.context <6.1.0
# Note: Airflow 2.9.x constraints pin jaraco.context==5.3.0, creating a conflict
# when installing acryl-datahub with Airflow. The airflow-plugin uses a constraints
# override in CI to work around this. This constraint is safe for Docker images
# which don't coexist with Airflow's pinned dependencies.
jaraco.context>=6.1.0,<7
# CVE-2026-45409: DoS via idna.encode() on crafted inputs (bypass of CVE-2024-3651 fix).
# Not in setup.py: Airflow constraints pin idna to older versions (e.g. 3.4, 3.10).
idna>=3.15,<4.0.0
# urllib3: CVE-2025-50181 (2.5.0); CVE-2025-66418, CVE-2025-66471 (2.6.0); CVE-2026-21441 (2.6.3);
# CVE-2026-44431 (2.7.0).
urllib3>=2.7.0,<3.0.0
# PyJWT: GHSA-xgmm-8j9v-c9wx (JWK-as-HMAC confusion); fixed in >=2.13.0
PyJWT>=2.13.0
# pyOpenSSL: CVE-2026-27459; 26.4.0+ required with cryptography>=49
pyopenssl>=26.4.0
# pyasn1: CVE-2026-30922, CVE-2026-59885, CVE-2026-59886; fixed in 0.6.4
pyasn1>=0.6.4
# protobuf (6.x): CVE-2026-0994
protobuf>=6.33.5,<7.0.0
# CVE-2025-30304, CVE-2025-32442: aiohttp request smuggling; keep current patch floor
# Note: Airflow 2.x constraints pin aiohttp==3.10.10 and 3.x pins 3.12.15, creating a
# conflict when installing acryl-datahub with Airflow. The airflow-plugin CI accepts the
# older version; this constraint is safe for Docker images which don't coexist with
# Airflow's pinned dependencies.
aiohttp>=3.13.4
# anyio: CVE-2026-64847 (process-pool workers block indefinitely on undrained
# stderr); fixed in 4.14.2. Not in setup.py (Airflow constraints pin 4.10–4.13);
# lock-time floor is [tool.uv] constraint-dependencies.
anyio>=4.14.2
# click: CVE-2026-7246 (8.1.7 / 8.3.1; fixed in 8.3.3). setup.py stays
# Airflow-satisfiable (3.0/3.1 pin 8.2.1, 3.2 pins 8.3.1); this Docker /
# lock-time floor is the CVE minimum.
click>=8.3.3,<9.0.0
pillow>=12.1.1
# CVE-2026-59890 (also covers CVE-2025-47273); fixed in setuptools 83.0.0.
setuptools>=83.0.0
# CVE-2026-69247; CVE-2026-69249 path-building DoS covered by 49+.
cryptography>=50.0.0,<51.0.0
# GHSA-6v7p-g79w-8964: Unpacker reuse after error can SEGV / DoS; fixed in 1.2.1.
msgpack>=1.2.1
# JWE RSA1_5 padding oracle (CVE); fixed in 1.6.9
# GHSA-jj8c-mmj3-mmgv: OAuth cache CSRF; fixed in >=1.6.11
# Previous Docker minimum: authlib>=1.6.9
authlib>=1.6.11
werkzeug>=3.1.6
azure-core>=1.38.0
# CVE-2026-25087: Apache Arrow C++ Use After Free; fixed in pyarrow 23.0.1. Docker minimum.
pyarrow>=23.0.1
# Docker minimum; airflow-plugin accepts older filelock from Airflow 2.7.3+ constraints for compatibility.
filelock>=3.20.3
nbconvert>=7.17.0
# pip: CVE-2026-3219, CVE-2026-6357 (fixed in 26.1); CVE-2026-13346 — the URL path was
# decoded twice when determining a link filename; fixed in 26.2.
pip>=26.2
wheel>=0.46.2
flask>=3.1.3
fonttools>=4.60.2
langchain-core>=1.2.22
python-multipart>=0.0.22
sqlparse>=0.5.4
starlette>=0.49.1
PyNaCl>=1.6.2
google-cloud-aiplatform>=1.133.0
langgraph>=1.0.10
langgraph-checkpoint>=4.0.0
langsmith>=0.6.3
# gitpython: GHSA-rpm5-65cw-6hj4; fixed in 3.1.47.
# gitpython: CVE-2026-42284 (crit; RCE via multi_options in _clone), CVE-2026-42215,
# CVE-2026-44243 (path traversal), CVE-2026-44244 (config injection) fixed in 3.1.49;
# GHSA-mv93-w799-cj2w fixed in 3.1.50.
# GHSA-2f96-g7mh-g2hx, GHSA-956x-8gvw-wg5v, GHSA-v396-v7q4-x2qj fixed in 3.1.51;
# GHSA-rwj8-pgh3-r573 fixed in 3.1.52. Floor 3.1.58 matches acryl-datahub setup.py.
gitpython>=3.1.58,<4.0.0
# CVE-2026-41066: lxml 6.0.x default entity resolution; fixed in 6.1.0. Major cap until reviewed.
# (unstructured / HTML parsing; bundled venvs such as datahub-documents-bundled.)
lxml>=6.1.0,<7.0.0
# mlflow-skinny: CVE-2025-14287, CVE-2025-15031, CVE-2026-2033 (fixed >=3.9.0)
mlflow-skinny>=3.9.0,<4.0.0
# ujson: parsing huge out-of-range integers leaked memory in 5.4.0–5.11.0; indent handling
# DoS (GHSA-c8rr-9gxc-jprv) fixed in 5.12.0. CVE-2026-44660 (dump() write-failure memory
# leak) fixed in 5.12.1. Cap major until reviewed.
ujson>=5.12.1,<6
# soupsieve: CVE-2026-49476, CVE-2026-49477 (memory exhaustion / ReDoS; 2.8.4),
# CVE-2026-85999 (ReDoS RE_WS_END), CVE-2026-86000 (ReDoS IDENTIFIER/VALUE);
# all fixed in 2.9.0. Transitive (beautifulsoup4); not in setup.py.
soupsieve>=2.9.0
