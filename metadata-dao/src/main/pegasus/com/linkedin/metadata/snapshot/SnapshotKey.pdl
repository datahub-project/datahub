namespace com.linkedin.metadata.snapshot

import com.linkedin.metadata.aspect.AspectVersion

/**
 * The Key for a metadata Snapshot.
 */
record SnapshotKey {

  /**
   * A list of metadata aspects in the Snapshot and their versions
   */
  aspectVersions: array[AspectVersion]
}