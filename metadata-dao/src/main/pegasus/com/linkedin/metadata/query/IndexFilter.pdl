namespace com.linkedin.metadata.query

/**
 * Filters for finding records in the index table
 */
record IndexFilter {

  /**
   * A list of criteria to filter records from the index table, AND being the logical operator
   */
  criteria: array[IndexCriterion]
}
