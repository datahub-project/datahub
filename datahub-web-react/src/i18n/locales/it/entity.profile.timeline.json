{
    "schemaTimeline.changeAdded": "{{fieldPath}} è stato aggiunto.",
    "schemaTimeline.changeModified": "{{fieldPath}} è stato modificato.",
    "schemaTimeline.changeRemoved": "{{fieldPath}} è stato rimosso."
}
