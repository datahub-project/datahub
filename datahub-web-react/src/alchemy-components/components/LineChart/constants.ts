// Disabling no hardcoded colors rule because this is a one off usage, need not be added to the theme
// eslint-disable-next-line rulesdir/no-hardcoded-colors
export const GLYPH_DROP_SHADOW_FILTER = `
    drop-shadow(0px 1px 3px rgba(33, 23, 95, 0.30))
    drop-shadow(0px 2px 5px rgba(33, 23, 95, 0.25))
    drop-shadow(0px -2px 5px rgba(33, 23, 95, 0.25)
`;
