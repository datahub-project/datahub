/**
 * Max queries to fetch on the Queries Tab
 */
const MAX_QUERIES_COUNT = 1000;
/**
 * Default page size for Queries Tab Section
 */
const DEFAULT_PAGE_SIZE = 6;
/**
 * The Max number of "Recent Queries" to surface.
 */
const DEFAULT_MAX_RECENT_QUERIES = 9;

/**
 * Searching Queries
 */
const MAX_ROWS_BEFORE_DEBOUNCE = 50;
export const HALF_SECOND_IN_MS = 500;

const ADD_UNAUTHORIZED_MESSAGE = 'You are not authorized to add Queries to this entity.';
