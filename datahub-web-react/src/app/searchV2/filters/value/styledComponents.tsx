import { Menu } from 'antd';
import styled from 'styled-components';

export const OptionMenu = styled(Menu)`
    &&& .ant-menu-item {
        height: auto;
        line-height: 22px;
        height: auto;
        margin: 0px;
    }
`;
