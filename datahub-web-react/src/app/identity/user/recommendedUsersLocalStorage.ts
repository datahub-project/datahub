/**
 * Stub for recommended users localStorage management.
 * OSS doesn't have the recommended users feature, so this is a no-op.
 */
export const markRecommendedUsersAsSeen = () => {
    // No-op in OSS
};
