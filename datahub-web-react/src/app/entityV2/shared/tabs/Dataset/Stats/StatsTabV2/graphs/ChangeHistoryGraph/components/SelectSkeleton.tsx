import { Skeleton } from 'antd';
import styled from 'styled-components';

export const SelectSkeleton = styled(Skeleton.Button)`
    .ant-skeleton-button {
        width: 145px;
        height: 42px;
    }
`;
