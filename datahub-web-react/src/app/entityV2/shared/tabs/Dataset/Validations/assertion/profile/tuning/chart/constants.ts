import { colors } from '@components';

/**
 * Chart styling constants
 */
export const METRICS_LINE_COLOR = colors.primary[500];
export const ANOMALY_COLOR = colors.red[500];
export const DEFAULT_AXIS_COLOR = colors.gray[100];
export const EXCLUSION_WINDOW_COLOR = colors.gray[500];
export const MARGIN = { top: 20, right: 30, bottom: 40, left: 60 };
