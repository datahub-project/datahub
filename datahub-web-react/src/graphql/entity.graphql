query getEntities($urns: [String!]!) {
    entities(urns: $urns) {
        urn
        type
        ...searchResultFields
    }
}

query getEntity($urn: String!) {
    entity(urn: $urn) {
        urn
        type
        ...searchResultFields
    }
}
