query getTotalDatasets(
    $input: SearchAcrossEntitiesInput!
    $skipSiblingsSearch: Boolean = false
    $skipLineage: Boolean = false
) {
    searchAcrossEntities(input: $input) {
        ...searchResults
    }
}
