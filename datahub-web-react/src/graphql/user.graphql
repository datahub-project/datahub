query getUser($urn: String!) {
    user(urn: $urn) {
        urn
        username
        info {
            active
            displayName
            title
            firstName
            lastName
            fullName
        }
        editableInfo {
            pictureLink
        }
    }
}
