mutation shareEntity($input: ShareEntityInput!) {
    shareEntity(input: $input) {
		succeeded
        share {
            lastShareResults {
                created {
                    time
                }
            }
        }
    }
}
