# Resuable Components

This directory contains generic components that can be imported into the project, standardizing components and styles.

You can use these items by importing theme items:

```tsx
import { Button } from './components';
```

and use them as:

```js
<Button />
```
