# Theme

This directory contains definitions for theme items such as colors, fonts, and spacings. 

You can use these items by importing theme items: 
```tsx
import { colors, radii, tokens } from './components/theme';
```

and referencing them as: 
```js 
colors.violet[100]
tokens.primary.color
radii.sm
```