pytest>=8.4.2
pytest-dependency>=0.6.0
pytest-rerunfailures==16.1
psutil
tenacity
-e ../metadata-ingestion[datahub-rest,datahub-kafka,mysql,postgres,datahub-documents]
pydantic>2
slack-sdk==3.18.1
aiohttp
joblib
pytest-xdist
networkx
croniter>=1.3.0
# libaries for linting below this
mypy==1.14.1
ruff==0.11.7
# stub version are copied from metadata-ingestion/setup.py and that should be the source of truth
types-requests>=2.28.11.6,<=2.31.0.3
types-PyYAML
types-croniter
# https://github.com/docker/docker-py/issues/3256
requests<=2.31.0
# Missing numpy requirement in 8.0.0
deepdiff!=8.0.0
opensearch-py==2.6.0

python-dotenv==1.1.1

# Required for inference_utils integration tests
-e ../datahub-executor[observe]

# Additional dependencies for telemetry API test
confluent-kafka[schemaregistry,avro]>=1.9.0,!=2.8.1
python-snappy>=0.6.1

# SaaS Only:
# The integrations service is NOT installed here to avoid a protobuf version
# conflict between datahub-executor[observe] (needs protobuf<5 via
# google-cloud-datacatalog-lineage==0.2.2) and datahub-integrations-service
# (needs protobuf>=5 via fastmcp -> opentelemetry).
# Propagation tests communicate with the integrations service over HTTP and
# don't need the package installed locally. The only test that imports from
# datahub_integrations (test_telemetry_api.py) is skipped when absent.
# required for bigquery docs propagation tests
-e ../metadata-ingestion[bigquery-slim]
-e ../datahub-actions

# For semantic search tests (embedding generation)
litellm>=1.0.0
