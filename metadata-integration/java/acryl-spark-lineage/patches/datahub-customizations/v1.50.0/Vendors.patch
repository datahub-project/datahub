# Patch for DataHub customizations in Vendors.java
# Upstream version: OpenLineage 1.50.0
# Generated: 2026-08-26 09:58:08 UTC
#
# To apply this patch to a new upstream version, from the acryl-spark-lineage module directory:
#   patch src/main/java/io/openlineage/spark/api/Vendors.java < patches/datahub-customizations/v1.50.0/Vendors.patch
#
# The target is named explicitly rather than left to -p<n>: both header paths exist during an
# upgrade, and patch's "fewest components wins" heuristic picks the upstream reference copy, so
# -p1 would silently rewrite patches/upstream-*/ instead of the source tree.
#
--- a/patches/upstream-1.50.0/spark/api/Vendors.java
+++ b/src/main/java/io/openlineage/spark/api/Vendors.java
@@ -22,7 +22,9 @@
           // Add vendor classes here
           "io.openlineage.spark.agent.vendor.snowflake.SnowflakeVendor",
           "io.openlineage.spark.agent.vendor.iceberg.IcebergVendor",
-          "io.openlineage.spark.agent.vendor.gcp.GcpVendor");
+          "io.openlineage.spark.agent.vendor.gcp.GcpVendor",
+          // This is the only chance we have to add the RedshiftVendor to the list of vendors
+          "io.openlineage.spark.agent.vendor.redshift.RedshiftVendor");
 
   static Vendors getVendors() {
     return getVendors(Collections.emptyList());
