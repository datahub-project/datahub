server.port=9091
management.endpoints.web.exposure.include=metrics, health, info
spring.mvc.servlet.path=/
management.health.elasticsearch.enabled=false
management.health.neo4j.enabled=false
entityClient.impl=restli
# Entity graph cache is GMS-only; shared application.yaml defaults enabled=true.
datahub.gms.entityGraphCache.enabled=false
# Usage aggregation is GMS-only; shared application.yaml may enable via USAGE_AGGREGATION_ENABLED.
datahub.usage.aggregation.enabled=false
