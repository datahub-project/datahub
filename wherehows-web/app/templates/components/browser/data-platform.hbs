{{#dynamic-link params=node}}
  <p class="data-platform__platform-name">
    <strong>
      {{platform.platform}}
    </strong>
  </p>

  <p class="data-platform__platform-count">
    {{#if platformTask.isRunning}}
      {{pendulum-ellipsis-animation}}
    {{else}}
      {{pluralize platform.count 'dataset'}}
    {{/if}}
  </p>
{{/dynamic-link}}