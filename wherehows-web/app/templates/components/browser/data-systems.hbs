{{#each nodes as |nodeParams|}}
  {{browser/data-system dataSystem=nodeParams}}
{{else}}
  {{empty-state heading="No records found" subHead="We could not retrieve any datasets at the specified data system"}}
{{/each}}
