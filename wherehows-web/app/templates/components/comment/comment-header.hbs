<header>
  {{#if hasBlock}}
    {{yield}}
  {{else}}
    {{#if comments.length}}
      <i class="fa fa-commenting" title="Comment Count"></i>

      {{comments.length}}
    {{/if}}
  {{/if}}
</header>
