# Login V2 Test Documentation

## Overview

- **Feature**: Login with Theme V2
- **Test Suite**: `tests/login-v2/`
- **Page Objects**: `pages/login-page.ts`
- **Total Tests**: 1
- **Last Updated**: 2026-04-10

---

## Test Coverage Summary

### By Priority

- **P0 (Critical/Smoke)**: 1 test

### By Type

- **Smoke**: 1 test

---

## Test Cases

### File: `tests/login-v2/v2-login.spec.ts`

Suite: `Login with Theme V2`

**Preconditions (beforeEach)**:

- Feature flags are intercepted via `apiMock.setFeatureFlags`:
  - `themeV2Enabled: true`
  - `themeV2Default: true`
  - `showNavBarRedesign: true`
- localStorage overrides injected via `page.addInitScript`:
  - `isThemeV2Enabled` → `false` (let server flags win)
  - `showHomePageRedesign` → `false`
  - `isNavBarRedesignEnabled` → `false`
  - `skipAcrylIntroducePage` → `true`
  - `skipWelcomeModal` → `true`

---

#### Test 1: logs in successfully with Theme V2 enabled

**Priority**: P0
**Type**: Smoke

**Objective**: Verify that login succeeds and the Theme V2 navigation bar ("Discover" button) is rendered after authentication.

**Prerequisites**:

- Fresh, unauthenticated browser context (provided by `login-test` fixture)
- Theme V2 feature flags activated via `apiMock`
- Admin credentials available via `resolvedUsers.admin`

**Steps**:

1. Navigate to `/`
2. Enter admin credentials via `loginPage.login()`

**Expected Results**:

- A button with the accessible name `Discover` is visible (Theme V2 nav bar indicator)

---

## Selectors Reference

| Element             | Selector                       | Page Object            | Purpose                               |
| ------------------- | ------------------------------ | ---------------------- | ------------------------------------- |
| Discover nav button | `role=button[name="Discover"]` | N/A (inline assertion) | Confirms Theme V2 nav bar is rendered |

---

## Page Objects Used

### LoginPage

**File**: `pages/login-page.ts`

**Key Methods**:

- `login(username, password)` — fill credentials and submit the login form

---

## Test Execution

### Run Login V2 Tests

```bash
cd e2e-test/ui/playwright
npx playwright test tests/login-v2/
```

### Run a Specific Test

```bash
npx playwright test tests/login-v2/v2-login.spec.ts -g "logs in successfully with Theme V2"
```

---

## Dependencies

### Required Services

- DataHub backend (default: `localhost:9002` or env `BASE_URL`)
- `apiMock` fixture to intercept GraphQL feature flag responses

### Fixtures Used

- `login-test` (from `fixtures/login-test.ts`) — unauthenticated context, `loginPage`, `apiMock`, `logger`, `logDir`

### Test Data

- Admin credentials: resolved from `fixtures/users.ts` → `resolvedUsers.admin`

---

## Known Issues / Limitations

- This suite contains a single smoke test — additional Theme V2-specific UI regression tests (navigation items, colour tokens, redesigned homepage) should be added as the feature matures.

---

## Related Documentation

- Base Auth Tests: `docs/test-suites/auth-tests.md`
- Onboarding Tests: `docs/test-suites/onboarding-tests.md`

---

_Generated by The Scribe on 2026-04-10_
